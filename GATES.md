# Human gates

The build loop pauses **dependent** work until the box is ticked; independent milestone work continues.
Tick a box (`- [x]`) and the loop picks it up within ~30 minutes. Each gate also fires a macOS notification when opened.

## Open gates

- ~~**gate-m1-aurservices-files**~~ — **CLOSED WITHOUT ACTION 2026-07-25 by Mirko's direct instruction: "No need for aur wiki files, remove that requirement, just use your golden set."** The AurServices wiki is no longer required on this machine, and no `acceptance.aurServicesWikiPath` is needed. M1's acceptance bar is now the golden corpus: it converts with zero errors and zero unknown-construct warnings, with a case for every construct in PLAN.md §7's table. **If M1's features are already green, M1 is CLOSED — verify against the goldens and move on; do not wait on this and do not reopen it.**
  Accepted cost, recorded so no iteration re-litigates it: the goldens are hand-authored from the same understanding that built the renderer, so they prove no regression but cannot discover a real-world dialect nobody predicted. The two discovery questions this gate existed to answer — how many pages use a fence dialect that now fails loud, and how many of the 59 mermaid diagrams use spellings `beautiful-mermaid` rejects (`graph TD;`, `pie`) — move to **M7 Aur adoption**, which is where the Aur files actually live. Full reasoning: `.claude/references/decisions.md`.

- [ ] **gate-m2-aur-review** (opened 2026-07-25 iter 42) — **M2's feature work is finished; its acceptance needs you.** Every named M2 deliverable is built and green: the Confluence client, the publish pipeline (upsert, attachments, hashing, banner, dry-run, changed-since, orphan report, prune, child-order, open-comment guard, `ac:width`) and now `docume status` (§6.6). What is left in the milestone is only its acceptance criterion — **revised 2026-07-25**: "publish DocuMe's own docs **plus the golden corpus** to a sandbox space, human-reviewed page-by-page" (PLAN.md §14). The Aur bulk publish moved to M7, so this gate no longer depends on the Aur files at all.
  **What to do, in order:** (1) the three sandbox items under "Setup Mirko must do before M2" below — create the `DOCUMESBX` space, set `confluence.sandboxSpaceKey`, set `confluence.sandboxBaseUrl`; (2) tick this box once you have walked the published sandbox pages and are satisfied with how they render. **That is the only remaining dependency — one space.**
  **What the loop does once (1) lands:** publishes DocuMe's own docs *and every golden case* to `DOCUMESBX`, and reports what to look at page by page. *(Shorter than it was, as of iter67: **DocuMe's own docs now exist** — `docs/wiki/`, 11 pages, commit 7b09165, held to a zero-failure **and** zero-degradation conversion bar by `DogfoodWikiTests`. Nothing is left to write; the run is `docume init --space DOCUMESBX --base-url <yours>`, then a `--dry-run --tree`, then the publish.)* *(Corrected iter66: there is **no config-loader slice** to build first. `confluence.baseUrl` is already a required field `ConfigLoader.Validate` checks — `src/DocuMe.Core/Config/ConfigLoader.cs:48` — and `docume init --base-url` already writes it into `docume.json`. The loop just passes your value through. Earlier wording promised a slice that does not need building.)* Publishing the goldens is the point: it is the first time the converter's output is rendered by real Confluence rather than asserted as a string, which is the one check the golden suite structurally cannot perform. It will not touch the production `AUR` space — that needs `confluence.productionAllowed: true` and **gate-m7-production**.
  **Why it is not blocking the loop right now:** M3 (§6.3 label sync, §6.5 dashboard, §8 approval state machine) is buildable offline against WireMock exactly as M2's client was, so the loop moves there and this gate waits.

  ---
  **THE PUBLISH IS DONE (2026-07-26, iter113). ONLY YOUR REVIEW IS LEFT — everything below is for you.**
  `DOCUMESBX` now holds **44 pages**, in three branches under `DOCUMESBX Home`. **43 are at version 1; `Documentation Status` is at v2 and 9 of the DocuMe pages now carry a `stale` label — iter121 did that deliberately and it is not damage.** It ran M5's live `drift --mark` (see `gate-m5-refresh-roundtrip` below): the label is accurate, because the repo really has moved 23 files since the publish sha, and adding it left every labelled page at **v1**, which is rule §9.3 holding against real Confluence rather than against a stub. Only the dashboard took a version, because refreshing it is a body write by definition. **Nothing about your review changes:** a label does not alter how a page renders, and the three unaffected pages (`Approval and Drift`, `Configuration and State`, `Open Questions for the Team`) are unlabelled, so the space also shows you a real mixed state instead of an all-zero one.
  - **DocuMe** (12 pages) — DocuMe's own `docs/wiki`, published from the committed root `docume.json`.
  - **Converter Golden Corpus** (30 pages) — 27 of the 28 `tests/golden/` cases, plus a README and the two link targets the corpus needs. Published from a *staged copy*: `tests/golden/` itself is untouched (rule §4.3), and the only edit was adding a `title:` to the 9 cases that carry none. Regenerate it with `python3 .mtk/paths-113/stage-golden-wiki.py`.
  - **Documentation Status** — the `docume dashboard` page (§6.5).

  **What to look at, in this order:**
  1. **Start at "DocuMe"** and walk its 12 pages. This is the product's own documentation and the thing a real consumer sees first. Check headings, tables, code blocks, panels, the injected banner, and the cross-page links between Concepts / Reference / Automation.
  2. **Open "The Documentation Lifecycle"** specifically — it carries the one mermaid diagram in DocuMe's own docs, rendered to SVG and uploaded as an attachment (`mermaid-02c4f9e7ccf0392c.svg`, 5.7 KB). Confirm the diagram is legible at the width it publishes at.
  3. **Then walk "Converter Golden Corpus".** Each page is one construct from PLAN.md §7. This is the check the golden suite structurally cannot do: `GoldenFileTests` asserts the storage XML byte-for-byte, but only Confluence can say whether that XML *renders* the way it was meant to. Pay attention to **Tables**, **Ragged tables**, **Lists**, **Nested Task Lists**, **Alerts**, **Nested Alerts**, **Code Fence Attributes** and **Toc**.
  4. **Two deliberate, documented losses to confirm you accept** (they are not bugs; `ConversionAcceptanceTests` pins them): on **Alerts**, `[!IMPORTANT]` and `[!NOTE]` render as the *same* info panel because storage format has no separate one; on **Tables**, a centre-aligned column publishes left-aligned.
  5. **One page is deliberately missing, and it is the finding of this run — see below.**

  **THE ONE THING THAT DID NOT PUBLISH, and the decision it needs from you.** `cases/mermaid.md` failed and is absent from the corpus. Its 4 diagrams are 4 different mermaid dialects, and the pinned renderer (`beautiful-mermaid` 1.1.3) rejects 2 of them. Measured directly, one dialect per run (`.mtk/paths-113/probe-mermaid-dialects.py`):
  - `flowchart LR` — **renders**; `sequenceDiagram` — **renders**.
  - `graph TD;` — **fails**, and *only because of the trailing semicolon*: the identical diagram with the header written `graph TD` renders fine. `beautiful-mermaid` matches the header exactly and does not tolerate the `;`, which mermaid itself accepts.
  - `pie` — **fails outright**, with or without a title. The renderer has no pie support at all.
  A page fails as a unit, so those 2 cost all 4 of its diagrams. `PublishExecutor.cs:306` catches a render failure and continues, which is why the other 30 pages published cleanly — only `Setup` faults abort a run.
  **This matters well beyond one golden case:** `graph TD` is the most common mermaid header there is, and `graph TD;` with the semicolon is a completely ordinary way to write it. The Aur wiki's 59 diagrams are very likely to contain both spellings, so this is the M7 adoption risk arriving early. **Three defensible answers, and it is your call — the loop will not pick one:** (a) upgrade or replace `beautiful-mermaid` (check whether a later version takes `graph TD;` and `pie`); (b) normalise the header in `tools/render-mermaid.mjs` — stripping a trailing `;` is a one-line fix that buys back `graph TD;`, though it does nothing for `pie`; (c) accept both as known unsupported dialects and have the converter report them as a named degradation rather than a hard page failure. Record your choice in `tools/loop/state.json → decisions`.

  **When you are satisfied with the rendering, tick this box.** The mermaid decision above does *not* have to be settled first; it is tracked separately and blocks no other milestone.

- [ ] **gate-m3-approval-roundtrip** (opened 2026-07-25 iter 44) — **M3's feature work is finished; its acceptance needs your hands on a label.** Every named M3 deliverable is built and green: label sync (`docume sync --labels`), the approval state machine (§8: record, invalidate, revoke, with history preserved), the `docume dashboard` page (§6.5), and the status table. What is left is only the acceptance criterion — "Sandbox e2e: approve → republish changed page → label removed → re-approve; dashboard correct" (PLAN.md §14) — and the loop cannot add a label as a human reviewer.
  **What to do, in order:** (1) the three sandbox items under "Setup Mirko must do before M2" below — the same ones **gate-m2-aur-review** is waiting on, so doing them once unblocks both; (2) let the loop publish to `DOCUMESBX`; (3) open one published page in Confluence and add the **`approved`** label to it; (4) tick this box.
  **What the loop does then:** runs `docume sync --labels` (which records the approval into `state.json`), republishes that page with a content change so §8 invalidates the approval and strips the label, regenerates the dashboard, and reports each step. You re-approve once more to close the cycle.
  **Expect `approvedBy` to read `unknown` — that is correct, not a bug.** Confluence Cloud exposes no label author anywhere the tool can reach (spike S3, settled iter43: not on CQL search results, not on `content/{id}/label`, not on v2 `/pages/{id}/labels`). Filling it with the account DocuMe authenticates as would put a fabricated approver in an audit trail, so it stays honest instead. The label itself is the gesture §8 keys on.
  **Why it is not blocking the loop right now:** §14 runs M4 and M5 in parallel after M3, and both are buildable offline, so the loop moved to M5 (§6.4 drift engine) and this gate waits.
  **Added iter122 — ticking this also unblocks the one live check `--notify-reviewers` needs.** §6.2 step 7's re-review comment shipped at iter122 (built, 1349 tests green, request shape pinned against WireMock), but it fires *only* where a run revokes an approval, and no page in `DOCUMESBX` carries an `approved` label yet — so the live half cannot be run by any iteration until step (3) above happens. When you republish that page in step (4)'s cycle, the loop will use `docume publish --notify-reviewers` so the comment is exercised in the same round trip. **Expect one extra footer comment on that page**, from the account DocuMe authenticates as, saying "Content updated since approval — please re-review". It spends no page version and `docume sync --comments` skips it (it is the bot's own).

- [ ] **gate-m4-feedback-roundtrip** (opened 2026-07-25 iter 51) — **M4's feature work is finished; its acceptance needs you to write one wrong comment.** Every named M4 deliverable is built and green: comment ingestion with cursors into `_meta/feedback/inbox/` (§6.3, §5.4), reply and resolve (`docume sync --reply`, §9 step 5), and the `/docs-feedback` skill plus its workflow (§9 steps 1–4). What is left is only the acceptance criterion — "Sandbox e2e: comment → inbox → fix PR → merged → reply posted" (PLAN.md §14) — and the loop cannot write a reviewer's comment for you.
  **What to do, in order:** (1) the three sandbox items under "Setup Mirko must do before M2" below — **the same ones gate-m2-aur-review and gate-m3-approval-roundtrip are waiting on, so doing them once unblocks all three**; (2) let the loop publish to `DOCUMESBX`; (3) open a published page and add a comment claiming something the page says is wrong (an *inline* comment on a specific sentence exercises the most code — the anchor, the resolve, and the dangling case); (4) tick this box.
  **What the loop does then:** runs `docume sync --comments` so your comment lands as an inbox item, runs `/docs-feedback` (which verifies your claim against the actual code, fixes the page if it holds and declines it with a citation if it does not) and opens a `docs/feedback-<date>` PR, merges it, republishes, then runs `docume sync --reply` so your comment gets answered and the inline one resolved.
  **Expect the reply to come from the account DocuMe authenticates as**, with a last line in italics saying it was posted automatically. Nothing you wrote is quoted back at you: a comment body is untrusted input (rule §1.3), and echoing it into a page is the thing that rule exists to prevent.
  **Why it is not blocking the loop right now:** M6 (§11 plugin, §12 packaging) is entirely buildable offline, so the loop moved there and this gate waits.

- [ ] **gate-m5-refresh-roundtrip** (opened 2026-07-26 iter 121) — **the gate that should have been opened at iter48 and was not. M5's feature work has been finished since then; nobody ever ran its acceptance.** Every named M5 deliverable is built and green (drift engine, github-comment format, stale labeling, the six §10 workflow templates, the `docs-refresh` skill), and iter48 recorded M5 as feature-complete and moved straight to M4. But §14's M5 acceptance — "Simulated code change → PR drift comment; deploy-sim → stale label → refresh PR → merge → auto-publish" — was never run, never gated, and is §15's unticked "A staging deploy marked pages stale and the nightly refresh PR regenerated them". **M5 was the only milestone whose acceptance had no gate**, so it read as done when it was not.
  **The middle of that chain is now DONE, live, and it is the half that did not need you.** iter121 ran `docume drift --mark` against `DOCUMESBX` with a real code change — the 23 files this repo actually changed between the publish sha `937c936e` and `HEAD` — and it labelled the 9 of 11 sourced pages whose declared `sources` those files matched. Evidence, all re-runnable: `python3 .mtk/paths-121/probe-labels.py` reads the labels and versions straight back from Confluence, `python3 .mtk/paths-121/probe-dashboard.py` reads the refreshed §6.5 dashboard, which agrees exactly (`⚠️ Stale 9`, and the same 3 pages unmarked). **The one thing this proved that 1342 passing tests could not:** the write footprint is asserted against WireMock by `CliDriftTests.The_whole_write_footprint_of_a_mark_run_is_two_labels_and_the_dashboard`, but only real Confluence can say whether *it* bumps a version when a label is added. It does not — all 9 pages read back at **v1**. Rule §9.3's "no page-version churn, no approval disturbance" is now measured, not assumed.
  **What is left needs you, and it is cheaper than it looks — the two remaining halves have different costs.**
  - **The PR drift comment needs NO secrets.** `docume drift` without `--mark` touches no credentials, no space and no network (that is deliberate — `DriftCommand.cs:23`), and `templates/workflows/docs-drift-pr.yml` posts with nothing but `secrets.GITHUB_TOKEN`. So this half needs only (a) that workflow installed on a repo and (b) one pull request to exist. **The loop cannot supply (b):** its whole discipline is direct commits to `main`, so it never opens a PR, and opening one on Moberg's org is yours.
  - **The refresh half does need secrets:** `docs-refresh.yml` runs `claude -p "/docs-refresh"` (an `ANTHROPIC_API_KEY`) and `docs-publish.yml` republishes (`DOCUME_CONFLUENCE_EMAIL` / `DOCUME_CONFLUENCE_TOKEN` as repo secrets). Installing those on this repo means every push to `main` publishes to the sandbox — a standing automation decision, not a one-off, which is why the loop did not make it for you.
  **What to do, in order:** (1) read the push blocker below first — it changes what a PR here even means; (2) decide whether M5's acceptance runs on *this* repo (copy `templates/workflows/docs-drift-pr.yml` into `.github/workflows/`, add the secrets you want) or whether you would rather it be folded into **M7 Aur adoption**, where a real repo with real PRs already exists; (3) tick this box when you are satisfied M5's chain has been demonstrated, or reword the criterion if you take the M7 route. **Either answer is fine and the loop will not pick one** — record it in `tools/loop/state.json → decisions.m5Acceptance`.
  **Why it is not blocking the loop:** M6 is feature-complete and M7 is production-gated, so nothing downstream of M5 is waiting on this.

- [ ] **gate-m6-first-release** (opened 2026-07-25 iter 58) — **M6's feature work is finished; its acceptance needs you to authorise the first release.** Every named M6 deliverable is built and green: the `docs-loop` skill (iter58, the last one), `plugin.json` + the marketplace entry, the release workflow, the README quickstart + `CHANGELOG.md`, and `init`'s thirteen templates. What is left is only the acceptance criterion — "fresh empty repo: full install story works end-to-end from README alone" (PLAN.md §14) — and **steps 1–2 of that README cannot work until a `v0.1.0` tag exists**, because the GitHub Packages feed is empty and `/plugin install` resolves a `ref` that is not a tag yet. Steps 3 onward already work from a source build.
  **Why the loop will not do this itself:** pushing the tag fires `.github/workflows/release.yml`, which packs and pushes `DocuMe.Cli` + `DocuMe.Core` to GitHub Packages and cuts a GitHub release. **A package on a feed cannot be unpublished.** That is an outward-facing, irreversible action on Moberg's org, so it is yours, not the loop's.
  **READ THIS BEFORE YOU TAG — found by iter121 and it changes step 2. `origin/main` IS 93 COMMITS BEHIND THIS MACHINE.** The remote is at `699c855` ("iter74"); local `main` is at iter120. `git status -sb` reads `[ahead 93]`, against a remote ref fetched the same day, so this is not a stale-ref illusion: **the loop has never pushed, and no iteration before this one recorded that.** Nothing was lost (the commits are all here), but two consequences bite at release time. (a) `git push origin v0.1.0` pushes the tag *and* every object it reaches, so the release itself would build correctly from the tag — while the repo's **default branch would still show iter74**, i.e. a `v0.1.0` release whose history is not on `main`. (b) Step 4's README edit, and every commit since iter74, would be invisible to anyone cloning the repo. **So push `main` first — `git push origin main` — then tag.** The loop did not push it for you: 93 commits landing on Moberg's org fires `ci.yml` (it triggers on push to `main`) and cannot be undone without the history rewrite rule §8.2 forbids, and no gate or protocol line authorises it. If you would rather the loop pushed routinely from now on, say so in `tools/loop/state.json → decisions.pushPolicy` and it will.
  **What to do, in order:** (1) confirm the three version-carrying files are all at the number you want to release — `Directory.Build.props` `<Version>`, `plugin/.claude-plugin/plugin.json` `version`, and the git-subdir `ref` in `plugin/README.md`; all three read `0.1.0` today and the workflow's *first* step refuses the release if any disagrees with the tag; (2) **`git push origin main`** (see the paragraph above), then `git tag v0.1.0 && git push origin v0.1.0`; (3) watch the run — the version guard runs before the build, so a mismatch costs nothing; (4) **delete the "Not published yet" note from `README.md` in the same commit or the one right after** (`QuickstartTests.The_unreleased_note_is_pinned_to_the_version_in_the_tree` keeps it naming the current version but cannot know whether the tag was pushed); (5) tick this box.
  **What the loop does then:** walks the README literally in a fresh empty directory — add the feed, install the tool from GitHub Packages, install the plugin from the marketplace, `docume init`, then the pre-publish checks — and reports every line that did not work as written. That walk is M6's acceptance, and it is the first time steps 1–2 are testable at all.
  **What it needs from you besides the tag:** a PAT with `read:packages` in the loop's environment if you want it to verify the *install* half rather than just the publish half; GitHub Packages authenticates every read, including public ones.
  **Why it is not blocking the loop right now:** two offline slices remain and neither needs a release — serializing the process-spawning test classes (the known full-suite flake) and wiring `docume sync --reply` into `docs-publish.yml`. The loop took those and this gate waits.

## Anticipated (for orientation, not yet open)

- **gate-m7-production** — permission to publish to the production AUR space + team onboarding (M7). Ticking this also requires setting `confluence.productionAllowed: true` in `tools/loop/state.json`.

## Setup Mirko must do before M2 (the loop will BLOCK on these otherwise)

**Decided 2026-07-24 (delegated to Claude): a dedicated disposable sandbox space on Moberg's own Confluence — NOT the existing `ITAPPS` space.** `publish --prune` deletes orphan pages and republish overwrites hand edits by design, so the loop must never point at a space holding human content or watchers. Full rationale and the accepted cross-tenant consequence: `.claude/references/decisions.md`. Only the clicks below are left; nothing here needs another judgement call.

- [x] **Create the space** on Moberg's Confluence. **Done 2026-07-26 (Mirko: "Space created").** Name `DocuMe Sandbox`, key `DOCUMESBX`, permissions restricted to Mirko so machine churn and machine comments never reach anyone's notification feed.
- [x] **Set the key:** **Done 2026-07-26 08:31** — `confluence.sandboxSpaceKey` is `"DOCUMESBX"` in `tools/loop/state.json`. **The key is assumed, not verified:** it is the value this checklist specified, set from an interactive session that has no Confluence credentials, so nothing has actually resolved it against the live space. A 404 on first lookup means the key is wrong — report the exact key tried and ask, do not guess another. A 401/403 means the token, not the key (see the API-token item below). Before the first write, sanity-check that the space is empty: the safety case for `publish --prune` rests on this being a dedicated disposable space.
- [x] **Set the sandbox base URL.** **Done 2026-07-25 21:07 (Mirko, mid-iteration):** `confluence.sandboxBaseUrl` is `https://moberg.atlassian.net/wiki`, which also confirms the sandbox tenant is `moberg.atlassian.net`. The sandbox is on a *different tenant* than production `AUR` (`kvika.atlassian.net`), and `state.json → confluence` had no field for this. *(Corrected iter66: no code change is needed to consume it. The tool has always taken a base URL — `confluence.baseUrl` in `docume.json`, required and validated at `src/DocuMe.Core/Config/ConfigLoader.cs:48`, written by `docume init --base-url`. This field is loop bookkeeping: it tells the loop which tenant to point the dogfood repo's `docume.json` at.)*
- [x] **Create an API token** for your *Moberg-site* account. **VERIFIED BY THE LOOP 2026-07-26 (iter112), not ticked by hand:** the credentials already exported in the loop's shell authenticate against `moberg.atlassian.net`. Evidence: a read-only `GET /wiki/api/v2/spaces?keys=DOCUMESBX` returned **200** (not 401/403), resolving `DOCUMESBX` to space id **451870722**. So the exported token is a Moberg-site token, not the Kvika/AUR one, and the cross-tenant worry recorded above is closed. Probe: `.mtk/paths-112/probe-space.py` (read-only, never writes, never logs the token).
- [x] **Export credentials, then RESTART the loop.** `DOCUME_CONFLUENCE_EMAIL` and `DOCUME_CONFLUENCE_TOKEN` must be exported in the shell that launches `tools/loop/docume-loop.sh`. **Done — verified 2026-07-25 (iter33):** the running loop has both vars. Evidence: a `docume publish` smoke run got past `ConfluenceCredentials.FromEnvironment()` (which throws when either is missing) and reached the space lookup. Which token/account it is has not been checked, and cannot be from inside the loop — if it is the Kvika/AUR token it will not work against a Moberg-site sandbox (cross-tenant), so the API-token item above may still be outstanding.

**NOTHING IS LEFT BLOCKING A LIVE SANDBOX RUN — all four setup items are done as of 2026-07-26 (iter112).** Space, key, base URL and token are all confirmed, the last of them by measurement rather than assumption (see the token item above). The sandbox resolves to space id **451870722** and holds exactly one page, **`DOCUMESBX Home` (id 451871005)** — the homepage Confluence auto-creates with every space, i.e. no human content, so the space is genuinely disposable and `publish --prune` is safe against it. That page id is also the natural `confluence.rootPageId` for the dogfood `docume.json`. The write path has been built and WireMock-tested since iter33; it now has a real space to smoke-test against, and the next iteration can go straight at the `gate-m2-aur-review` acceptance run.

## Setup — not milestone-blocking, but do it soon

- [x] **setup-claude-dir** (opened 2026-07-24 iter 1; done 2026-07-24 in an interactive session) — MTK bootstrap finalized: the staged `.claude-proposed/` config was promoted into `.claude/` (rules, references incl. Moberg coding-guidelines, settings, tech-stack, mtk-version) and `.claude-proposed/` removed. Claude Code now auto-loads `.claude/rules/` and MTK resolves `.claude/references/`. The headless loop still cannot *write* `.claude/` (sensitive-file protection) but reads it fine, so no loop change was required.
  Optional, to let future loop iterations maintain `.claude/` themselves: add `"Write(.claude/**)"` and `"Edit(.claude/**)"` to `permissions.allow` in `tools/loop/loop-settings.json`. **Added 2026-07-25 (commit 6db1c39), and iter75 proved it does not work:** the Edit tool still refuses `.claude/**` as a sensitive file, so that allowlist entry does not reach `.claude/`. The line above stays true as written — `.claude/` is read-only to the loop, from both a headless and an interactive session.

- [ ] **paste-rule-8-2a** (opened 2026-07-25 iter 75) — **one paste, no drafting, nothing waits on it.** Add the `§8.2a` bullet to `.claude/rules/git-workflow.md` directly beneath `§8.2`, leaving `§8.2` itself untouched. **The exact text is already written out verbatim** in `tools/loop/rule-8-2a-paste.md` — open that file and copy the bullet below the `---`. *(It lived in `tools/loop/state.json → decisions.compositeActionNote` until iter128, where it was a JSON string full of `\n` escapes; a 3.7 KB field whose only job is to be pasted had the least pasteable shape possible.)*
  Why it exists: you authorized the composite action's `v1` floating tag to be force-moved by `.github/workflows/release.yml` (`decisions.compositeAction: "floating-v1"`), and iter75 built exactly that (commit `db240dc`). But `§8.2` still reads "NEVER force-push or rewrite history" with no exception, so a future iteration could read it, conclude that step is a violation, and delete work you asked for. The rule text scopes the exception to that one tag, on the runner, and restates that branch history stays untouchable.
  Why the loop did not do it: `.claude/` is refused to it (see above). It *could* have been written through an allowlisted `python3`, and that was deliberately not done — the content is an agent-facing rule that widens force-push authority, and the guard in the way exists to stop an agent installing precisely that. If you would rather the rule stayed unwritten, say so and the loop will stop carrying the ask; nothing breaks either way.

- [ ] **paste-force-push-hook** (opened 2026-07-26 iter 130) — **one paste, no drafting, and it closes a hole in a rule that says `[ENFORCED]`.** Replace the whole of `tools/loop/loop-settings.json` with the block in `tools/loop/loop-settings-paste.md` (open it and copy the single fenced `json` block; the file also carries the measurement table and the reasoning).
  What it fixes: `§8.2`'s enforcement half is that file's deny list, and its patterns match whole tokens **from the start of the command**, so they only cover the spelling where the flag comes first. Measured at iter130 against a **nonexistent remote name**, so no probe could reach GitHub or push anything: `git push --force <remote> HEAD` → **denied**; `git push <remote> HEAD --force` → **allowed**; `git push <remote> HEAD -f` → **allowed**; `git push --force-with-lease <remote> HEAD` → **allowed**. With `origin main` in place of that remote, row two would have rewritten `origin/main` and §8.2 says that cannot be undone. No deny list closes it — a remote and a refspec of any name sit between `git push` and the flag, so there is no prefix to match — which is why the fix is a `PreToolUse` hook: `tools/loop/hooks/deny-history-rewrite.py`, committed, inert until this paste wires it, proven both directions 25/25 by `python3 .mtk/paths-130/mutate-force-push-guard.py`.
  Why the loop did not do it: `Edit` on `tools/loop/loop-settings.json` was **refused** under `acceptEdits`, while a `Write` of a new file under `tools/loop/hooks/` succeeded in the same session — the guard is specific to the settings file. Routing around it with an allowlisted `python3` was deliberately not done, on the `paste-rule-8-2a` precedent: this change only *narrows* authority, but anything that can add a `PreToolUse` hook to its own settings can also add one that auto-approves, and that is exactly what the guard is there to stop. `.mtk/paths-130/validate-settings-paste.py` asserts the block adds no `allow` entry, drops none, and keeps every existing `deny` (45 allow / 9 deny, live 45 / 6).
  **PROVEN WORKING BEFORE YOU INSTALL IT (iter131).** iter130 left the central question open — *does a hook declared in a `--settings` file actually load and fire?* — and answered it "the iteration after Mirko pastes". It did not need the paste: `python3 .mtk/paths-131/probe-hook-from-settings.py` hands **the exact fenced block below** to a child `claude -p` session as a throwaway scratch settings file and runs three children, **3/3** — (A) paste settings + `git push deny-probe-nonexistent-remote HEAD --force` → the hook **refused it** with the §8.2 message; (B) control, today's live settings, same command → **reached git** and died on the nonexistent remote, reproducing the hole exactly; (C) paste settings + `git status --short` → **ran clean**, so the guard is selective and not a blanket block. Nothing was installed: the scratch file lives under `.mtk/`, the loop's own settings were never touched, and the probe refuses to run at all if the scratch would add an `allow` entry. So this is a tested fix, not a hopeful one. After you paste, `git push deny-probe-nonexistent-remote HEAD --force` is still worth running once as the in-situ confirmation.
  **Until it is pasted the hole is open**, and what stands in its place is that the loop has never pushed anything in 130 iterations (`origin/main` is still at iter74) plus the flag-first denies, which do work.

- [ ] **settings-corrections** (opened 2026-07-26 iter 131) — **two one-line fixes on your side, found by reading the CLI's own stderr, which every loop iteration has been printing unread.** Neither blocks anything; the second one is the one worth your attention because it is about `.env` files, not about this loop.
  **(a) This workspace is not trusted.** Every iteration the CLI prints: *"Ignoring 3 permissions.allow entries from `.claude/settings.json`: this workspace has not been trusted."* `~/.claude.json` has `projects["/Users/mirkobudimir/Dev/docu-me"].hasTrustDialogAccepted: false` — 25 of your 43 recorded projects are trusted; this one never was. The three ignored entries are `Bash(dotnet build:*)`, `Bash(dotnet test:*)`, `Bash(dotnet format:*)` — **the protocol's own verification commands**. *Cost today: zero* — `tools/loop/loop-settings.json` supplies the broader `Bash(dotnet:*)` through `--settings`, which is honoured, and that is why 130 iterations built and tested fine. But `.claude/settings.json`'s allow half is inert in the loop (the analogue of iter89's dead-allow finding), so anything added there later will silently not apply. **Fix:** run Claude Code interactively in this repo once and accept the trust dialog, or set that key to `true`. **Then:** the loop stops depending on `--settings` alone for its build commands; nothing else changes. *(Deliberately not done by the loop: `~/.claude.json` is your global state, outside the repo, and trusting a workspace is a security decision, not bookkeeping.)*
  **(b) Two deny rules in your GLOBAL settings do nothing, and they are the `.env` ones.** The CLI reports: *"Permission deny rule (`../../.claude/settings.json`): `Write(.env)` is not matched by file permission checks — only `Edit(path)` rules are. Use `Edit(.env)` instead (Edit rules cover all file-editing tools)."* Same for `Write(.env.*)`. So `~/.claude/settings.json` intends "never write `.env` files" and, as spelled, **enforces nothing** — `Write(path)` is not a shape the file-permission check matches. **Fix:** change those two entries to `Edit(.env)` and `Edit(.env.*)`. Related to rules §0.3/§1.1 (credentials in env vars, never in committed files), which is why it is being surfaced rather than filed away. **What the loop will NOT do, and please do not ask it to:** the same diagnostic fires for `Write(.claude/**)` in `tools/loop/loop-settings.json`, and the CLI's suggested `Edit(.claude/**)` is precisely the rewrite that would hand the loop write access to its own rules — dead as written is the *correct* state for that one, so `loop-settings-paste.md` carries it forward unchanged and iter89's conclusion stands.
