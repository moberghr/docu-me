# Plan — M0 Scaffold Batch

Spec: `docs/specs/2026-07-24-m0-scaffold.md`. Source: `PLAN.md` §3, §4, §14 (M0).

## Rigor

STANDARD (score ~4: 1 batch, ~9 files most mechanical scaffold, security_impact=none, 0 external contracts, new-feature scope). Inline implementation path. No hard-trigger floor hit (1 batch < 3; non-mechanical files < 6). Conditional Stage-2 reviewers: scaffold introduces no behavior and no cross-slice boundary → self-review against coding-guidelines in lieu of subagent reviewers.

## Steps

1. **Solution + projects** (dotnet CLI):
   - `dotnet new sln -n DocuMe --format slnx`
   - `dotnet new classlib -n DocuMe.Core -o src/DocuMe.Core`
   - `dotnet new console -n DocuMe.Cli -o src/DocuMe.Cli`
   - `dotnet new xunit -n DocuMe.Core.Tests -o tests/DocuMe.Core.Tests`
   - add all three to the solution.
2. **Directory.Build.props + global.json** — centralize net10.0, nullable, implicit usings, langversion, warnings-as-errors; pin SDK.
3. **CLI project**: edit csproj for tool packaging (`PackAsTool`, `ToolCommandName=docume`, PackageId), add ProjectReference to Core, add System.CommandLine + Spectre.Console packages. Write `Program.cs` (RootCommand + built-in `--version`). Remove template `Class1.cs` from Core.
4. **Tests**: add ProjectReference to Core, add Shouldly + NSubstitute packages, replace template test with `ScaffoldSmokeTests`.
5. **CI**: `.github/workflows/ci.yml` — setup-dotnet 10.0.100, restore, build `-warnaserror`, test.
6. **Verify**: `dotnet build`, `dotnet test`, `dotnet run --project src/DocuMe.Cli -- --version`.
7. **Commit** to main; update `tools/loop/state.json`.

## Boundary

Only files in the spec's change manifest. No config/state/init logic this batch (deferred). No Confluence code.
