{
  "setup-claude-dir": "done",
  "gate-m1-aurservices-files": "CLOSED WITHOUT ACTION 2026-07-25 by Mirko (\"No need for aur wiki files, remove that requirement, just use your golden set\"), and M1 ITSELF IS NOW CLOSED — iter48 verified the revised rule §4.4 bar and enforced both of its halves (commit bcf36ba). EVIDENCE: GoldenFileTests 28/28 cases convert byte-for-byte to their hand-reviewed .storage.xml; ConversionAcceptanceTests 13/13 over tests/golden (zero failed pages, exactly the recorded degradations, and no count hidden when those codes are accepted); GoldenCoverageTests 3/3 maps every PLAN.md §7 construct row to the golden case(s) that pin it, in both directions. THE CHECK FOUND A REAL GAP: §7's markers row (⚠️ UNVERIFIED / AMBIGUOUS passed through as text) and its footer row (*Generated … by the docs loop* passed through italic) had no golden case at all, so two were hand-authored expectation-first (rule §4.3) and both matched the converter on the first run — the rows were implemented, just unasserted. Adding a §7 row without its case now fails GoldenCoverageTests. Accepted cost, do not re-litigate: hand-authored goldens prove no regression but cannot discover a dialect nobody predicted; that discovery (fence dialects, and how many of the 59 mermaid diagrams beautiful-mermaid rejects) moved to M7 Aur adoption. DO NOT REOPEN, and do not add titles to the goldens to satisfy `docume convert tests/golden` — that command loads the corpus as a publishable wiki tree and is not the bar.",
  "gate-m2-aur-review": "pending — BUT THE LOOP'S HALF IS FINISHED AS OF 2026-07-26 (iter113); ONLY MIRKO'S PAGE-BY-PAGE\nREVIEW REMAINS. OPENED iter42; ACCEPTANCE REVISED 2026-07-25; setup completed iter112; THE PUBLISH\nRAN AT ITER113 (commit 5429c0d). The bar was 'publish DocuMe's own docs PLUS THE GOLDEN CORPUS to a\nsandbox space, human-reviewed page-by-page' — both halves are now up. DOCUMESBX holds 44 pages,\nevery one at version 1: 'DocuMe' (12), 'Converter Golden Corpus' (30) and the dashboard.\nDO NOT RE-PUBLISH; re-running is a no-op but the gate is not waiting on it. GATES.md carries the\nordered review script. ONE CAVEAT THE REVIEWER MUST BE TOLD AND GATES.md STATES: cases/mermaid.md\nis deliberately absent — beautiful-mermaid 1.1.3 rejects 2 of its 4 dialects (`graph TD;` for the\ntrailing semicolon alone, and `pie` outright), and a page fails as a unit. That is a recorded\nfinding with three written options, not a blocker on this gate: Mirko can tick the box on the\nrendering he CAN see without settling the mermaid question first.",
  "gate-m3-approval-roundtrip": "pending (OPENED iter44) — **PREREQUISITE (b) IS NOW SATISFIED as of iter113: the loop has published\nto DOCUMESBX (44 pages, commit 5429c0d), so the only thing left is step (c), your hands on a label.\nPick any page under 'DocuMe' — 'CLI Reference' (id 451969089) is a good one.** — Mirko: M3 is feature-complete and only its acceptance criterion is left, which the loop cannot do alone because it needs a HUMAN to add a real label in a REAL space. It needs, in order: (a) the three sandbox items in GATES.md \"Setup Mirko must do before M2\" — create the DOCUMESBX space, set confluence.sandboxSpaceKey and confluence.sandboxBaseUrl in this file; (b) publish something there (the loop does this once the space exists); (c) YOU add the `approved` label to one published page in Confluence, then tick the box in GATES.md. The loop then runs `docume sync --labels` (records the approval), republishes that page with a content change (§8 invalidates it and removes the label), and you re-approve — that full cycle plus a correct dashboard is §14's M3 acceptance. NOTE approvedBy will read \"unknown\" and that is correct, not a bug: Confluence exposes no label author anywhere DocuMe can reach (spike S3, settled iter43), and filling it with the bot account would fabricate an audit trail. This gate blocks NO current loop work: M4 and M5 are parallel per §14 and both are buildable offline, so the loop moved to M5 §6.4.\n\nADDED ITER122 — THIS GATE NOW ALSO GATES ONE PIECE OF SHIPPED CODE. `docume publish --notify-reviewers`\n(§6.2 step 7's re-review comment) was built at iter122 and is pinned against WireMock, but it fires ONLY\nwhere a run revokes an approval, and no DOCUMESBX page carries an `approved` label — so no iteration can\nexercise it live until step (c) happens. When the box is ticked, the republish in this cycle must use\n`docume publish --notify-reviewers`, and ONE EXTRA FOOTER COMMENT is expected on that page, from the\naccount DocuMe authenticates as, reading \"Content updated since approval — please re-review\". It spends no\npage version, and `docume sync --comments` skips it because it is the bot's own.",
  "gate-m4-feedback-roundtrip": "pending (OPENED iter51) — **PREREQUISITE (b) IS NOW SATISFIED as of iter113: the loop has published\nto DOCUMESBX (44 pages, commit 5429c0d), so the only thing left is step (c), your comment. An\nINLINE comment on a specific sentence exercises the most code.** — Mirko: M4 is feature-complete and only its acceptance criterion is left, which the loop cannot do alone because it needs a HUMAN to write a real comment on a real page. Every named M4 deliverable is built and green: comment ingestion + cursors + inbox (§6.3, §5.4), reply/resolve (`sync --reply`, §9 step 5), and now the /docs-feedback SKILL.md + its workflow (§9 steps 1-4, iter51). What is left is §14's \"Sandbox e2e: comment → inbox → fix PR → merged → reply posted\". It needs, in order: (a) the three sandbox items in GATES.md \"Setup Mirko must do before M2\" — THE SAME ONES gate-m2-aur-review and gate-m3-approval-roundtrip wait on, so doing them once unblocks all three; (b) the loop publishes to DOCUMESBX; (c) YOU add a comment to a published page saying something factually wrong about it, then tick the box. The loop then runs `docume sync --comments` (item lands in _meta/feedback/inbox/), runs /docs-feedback (verifies the claim against the code and opens a docs/feedback-<date> PR), merges it, republishes, and runs `docume sync --reply` so your comment gets answered and — if you made it an inline comment — resolved. NOTE the reply is posted by the account DocuMe authenticates as, and its last line says so in italics. This gate blocks NO current loop work: M6 (§11, §12) is entirely buildable offline and the loop moved there.",
  "gate-m6-first-release": "pending (OPENED iter58) — Mirko: M6 is feature-complete and only its acceptance is left, and unlike the other three gates this one is not about a Confluence space — it is about cutting the FIRST RELEASE. §14's M6 acceptance is \"fresh empty repo: full install story works end-to-end from README alone\", and README steps 1-2 (add the GitHub Packages feed, install DocuMe.Cli; install the plugin from the marketplace) CANNOT WORK until a v0.1.0 tag exists: the feed is empty and the marketplace ref points at a tag that is not there. Steps 3 onward already work from a source build. WHY THE LOOP WILL NOT DO IT: pushing the tag fires .github/workflows/release.yml, which packs and pushes DocuMe.Cli + DocuMe.Core to GitHub Packages and cuts a GitHub release, and a package on a feed CANNOT BE UNPUBLISHED — outward-facing and irreversible on Moberg's org, so it is yours. WHAT TO DO: (1) confirm the three version-carrying files are at the number you want — Directory.Build.props <Version>, plugin/.claude-plugin/plugin.json version, and the git-subdir ref in plugin/README.md; all three read 0.1.0 today and release.yml's FIRST step refuses the release if any disagrees with the tag, before the build, so a mismatch costs nothing; (2) git tag v0.1.0 && git push origin v0.1.0; (3) DELETE THE \"Not published yet\" NOTE FROM README.md (QuickstartTests keeps it naming the current version but cannot know whether the tag was pushed); (4) tick the box in GATES.md. WHAT THE LOOP DOES THEN: walks the README literally in a fresh empty directory and reports every line that did not work as written — that walk IS M6's acceptance. IT ALSO NEEDS a PAT with read:packages in the loop environment if you want the INSTALL half verified rather than just the publish half; GitHub Packages authenticates every read, including public ones. This gate blocks NO current loop work: the flake-serialization fix and the sync-reply wiring slice are both offline.",
  "gate-m5-refresh-roundtrip": "pending (OPENED iter121, and it should have been opened at iter48) — Mirko: M5 has been feature-complete since iter48 but its §14 acceptance was never run and never gated; it was the ONLY milestone acceptance with no gate, which is why a half-finished milestone read as done. iter121 ran the live middle of the chain — `docume drift --mark` against DOCUMESBX on the 23 real files changed between publish sha 937c936e and HEAD — labelling 9 of 11 sourced pages, and PROVED AGAINST REAL CONFLUENCE that a label does not bump a page version (all 9 still v1, rule §9.3 measured rather than assumed; only the dashboard took v2). Probes: .mtk/paths-121/probe-labels.py, .mtk/paths-121/probe-dashboard.py. WHAT IS LEFT NEEDS MIRKO, and the two halves cost differently: the PR-drift-comment half needs NO SECRETS (drift without --mark uses no credentials, DriftCommand.cs:23; docs-drift-pr.yml posts with secrets.GITHUB_TOKEN) but needs a PULL REQUEST, which this loop never opens because it commits straight to main; the refresh half needs an ANTHROPIC_API_KEY plus Confluence repo secrets, which is a standing automation decision. THE DECISION: run M5's acceptance on this repo, or fold it into M7 where a real repo with real PRs exists — record it in decisions.m5Acceptance. Blocks nothing downstream: M6 is feature-complete and M7 is production-gated.",
  "settled-setup-before-m2": "## Setup Mirko must do before M2 (the loop will BLOCK on these otherwise)\n\n**Decided 2026-07-24 (delegated to Claude): a dedicated disposable sandbox space on Moberg's own Confluence — NOT the existing `ITAPPS` space.** `publish --prune` deletes orphan pages and republish overwrites hand edits by design, so the loop must never point at a space holding human content or watchers. Full rationale and the accepted cross-tenant consequence: `.claude/references/decisions.md`. Only the clicks below are left; nothing here needs another judgement call.\n\n- [x] **Create the space** on Moberg's Confluence. **Done 2026-07-26 (Mirko: \"Space created\").** Name `DocuMe Sandbox`, key `DOCUMESBX`, permissions restricted to Mirko so machine churn and machine comments never reach anyone's notification feed.\n- [x] **Set the key:** **Done 2026-07-26 08:31** — `confluence.sandboxSpaceKey` is `\"DOCUMESBX\"` in `tools/loop/state.json`. **The key is assumed, not verified:** it is the value this checklist specified, set from an interactive session that has no Confluence credentials, so nothing has actually resolved it against the live space. A 404 on first lookup means the key is wrong — report the exact key tried and ask, do not guess another. A 401/403 means the token, not the key (see the API-token item below). Before the first write, sanity-check that the space is empty: the safety case for `publish --prune` rests on this being a dedicated disposable space.\n- [x] **Set the sandbox base URL.** **Done 2026-07-25 21:07 (Mirko, mid-iteration):** `confluence.sandboxBaseUrl` is `https://moberg.atlassian.net/wiki`, which also confirms the sandbox tenant is `moberg.atlassian.net`. The sandbox is on a *different tenant* than production `AUR` (`kvika.atlassian.net`), and `state.json → confluence` had no field for this. *(Corrected iter66: no code change is needed to consume it. The tool has always taken a base URL — `confluence.baseUrl` in `docume.json`, required and validated at `src/DocuMe.Core/Config/ConfigLoader.cs:48`, written by `docume init --base-url`. This field is loop bookkeeping: it tells the loop which tenant to point the dogfood repo's `docume.json` at.)*\n- [x] **Create an API token** for your *Moberg-site* account. **VERIFIED BY THE LOOP 2026-07-26 (iter112), not ticked by hand:** the credentials already exported in the loop's shell authenticate against `moberg.atlassian.net`. Evidence: a read-only `GET /wiki/api/v2/spaces?keys=DOCUMESBX` returned **200** (not 401/403), resolving `DOCUMESBX` to space id **451870722**. So the exported token is a Moberg-site token, not the Kvika/AUR one, and the cross-tenant worry recorded above is closed. Probe: `.mtk/paths-112/probe-space.py` (read-only, never writes, never logs the token).\n- [x] **Export credentials, then RESTART the loop.** `DOCUME_CONFLUENCE_EMAIL` and `DOCUME_CONFLUENCE_TOKEN` must be exported in the shell that launches `tools/loop/docume-loop.sh`. **Done — verified 2026-07-25 (iter33):** the running loop has both vars. Evidence: a `docume publish` smoke run got past `ConfluenceCredentials.FromEnvironment()` (which throws when either is missing) and reached the space lookup. Which token/account it is has not been checked, and cannot be from inside the loop — if it is the Kvika/AUR token it will not work against a Moberg-site sandbox (cross-tenant), so the API-token item above may still be outstanding.\n\n**NOTHING IS LEFT BLOCKING A LIVE SANDBOX RUN — all four setup items are done as of 2026-07-26 (iter112).** Space, key, base URL and token are all confirmed, the last of them by measurement rather than assumption (see the token item above). The sandbox resolves to space id **451870722** and holds exactly one page, **`DOCUMESBX Home` (id 451871005)** — the homepage Confluence auto-creates with every space, i.e. no human content, so the space is genuinely disposable and `publish --prune` is safe against it. That page id is also the natural `confluence.rootPageId` for the dogfood `docume.json`. The write path has been built and WireMock-tested since iter33; it now has a real space to smoke-test against, and the next iteration can go straight at the `gate-m2-aur-review` acceptance run.\n\n",
  "trimmed-iter145": "TRIMMED-AT-ITER145: the four side-item mirrors below (restart-loop-driver, paste-rule-8-2a, paste-force-push-hook, paste-format-on-edit-hook) are the verbose `gates` values as they stood at iter144, moved here verbatim when iter145's done entry put state.json 511 tok over its budget. Nothing was discarded and the round trip was asserted before state.json was rewritten. GATES.md remains authoritative and was not touched; every key kept its PENDING status in the mirror so check_gate_mirror still sees it. ITER161 FOOTNOTE, recorded because a checker now pairs this file with `gates`: the note above says FOUR mirrors were moved here, and only THREE of those stubs still cite this file. iter155 rewrote the paste-format-on-edit-hook stub when Mirko answered formatOnEditHook \"delete\", and the rewrite dropped its \"Long mirror\" pointer. NOTHING WAS DESTROYED - the body is still keyed here and this sentence is the record; the pointer was not restored because state.json sits ~96 tok from its budget. check_gates_archive in tools/loop/check-state-size.py is what would have caught it had the BODY gone instead of the pointer.",
  "restart-loop-driver": "PENDING, MIRKO'S, THREE COMMANDS, AND IT IS THE REASON ITER137'S FIX HAS NEVER RUN. The driver process started 2026-07-24 14:38:30 and bash parses a `while` loop once, so the loop is executing a driver 56.6 h older than docume-loop.sh's newest commit; c6fbf1e and 7de6a86 (iter137's whole deliverable) are both dead code. Proof from its own output: log line `Iteration 165: fresh session` and filename `iter-0164-<ts>.log` are first-version formats. `touch tools/loop/STOP` -> wait -> `rm tools/loop/STOP` -> `./tools/loop/docume-loop.sh`. NOT blocking the pastes: ITERATION-PROMPT.md and loop-settings.json are re-read by path every pass. **TESTED AT ITER140, 9/9 (.mtk/paths-140/probe-restart-readiness.py): every function the restart wakes works, including all four degraded state-file shapes; `bash -n` parses clean; the file is committed 100755. NOTHING IS PENDING — the code is correct, it just is not running.** One cosmetic change to expect: modelUsage goes from `claude-opus-5[1m]` to `claude-opus-5`, same canonicalModel, same 1,000,000 contextWindow, same maxOutputTokens.",
  "paste-rule-8-2a": "PENDING, MIRKO'S, ONE PASTE, NOTHING WAITS ON IT — AND IT WAS MISSING FROM THIS BLOCK FROM ITER75 TO ITER144, WHICH IS WHY check-state-size.py NOW DERIVES THIS MIRROR FROM GATES.md. Add the §8.2a bullet to .claude/rules/git-workflow.md directly beneath §8.2, leaving §8.2 itself untouched; the exact text is written out verbatim in tools/loop/rule-8-2a-paste.md, below the `---`. It SCOPES the force-push exception you already authorised (decisions.compositeAction: floating-v1, built at db240dc — release.yml force-moves the composite action's floating `v1` tag) so a future iteration cannot read §8.2's unqualified 'NEVER force-push' literally and delete work you asked for. The loop will not write it itself even though an allowlisted python3 could: it is an agent-facing rule that WIDENS force-push authority, and the guard in the way exists to stop an agent installing exactly that (the iter89 line — `.claude/references/` is descriptive and fair game, `.claude/rules/` is behaviour-governing and stays yours). Full text and reasoning: GATES.md -> paste-rule-8-2a.",
  "paste-force-push-hook": "DONE 2026-07-27 (applied in an interactive session, not by the loop and not by Mirko). tools/loop/loop-settings.json now carries 3 extra deny spellings (--force-with-lease, --force-if-includes, --mirror) plus the PreToolUse guard tools/loop/hooks/deny-history-rewrite.py. VERIFIED AFTER WRITING: 45 allow / 9 deny (none added, none lost); mutate-force-push-guard.py 25/25; .mtk/paths-130/validate-settings-paste.py PASS. No driver restart was needed - this file is re-read by path every pass - so §8.2's [ENFORCED] label is now true for flag-anywhere spellings too. Remaining in-situ check: `git push deny-probe-nonexistent-remote HEAD --force` should be refused with the §8.2 message.\n\nWHY AN INTERACTIVE SESSION COULD DO WHAT THE LOOP COULD NOT: the refusal on this file was an acceptEdits-mode guard, not the path classifier. The two .claude/ pastes were re-attempted in the same session and REFUSED again (paste-format-on-edit-hook, paste-rule-8-2a) - that classifier sits above the permission layer and does not care which session asks. Routing either around an allowlisted python3 was declined, on the loop's own reasoning: a thing that can add a PreToolUse hook to its own settings can add one that auto-approves.\n\nVERBATIM GATES.md BODY AS IT READ WHILE OPEN (iters 130-168), compressed to a stub 2026-07-27 for step-1 budget; nothing here was discarded:\n- [x] **paste-force-push-hook** (opened 2026-07-26 iter 130; **APPLIED 2026-07-27 in an interactive session, not by the loop**) — the paste is in: `tools/loop/loop-settings.json` now carries the three extra `deny` spellings and the `PreToolUse` guard. Verified after writing: 45 allow / 9 deny (none added, none lost), `mutate-force-push-guard.py` **25/25**, `.mtk/paths-130/validate-settings-paste.py` **PASS**. The running driver re-reads this file by path every pass, so **no restart was needed for it to take effect**. Still worth running `git push deny-probe-nonexistent-remote HEAD --force` once as the in-situ confirmation. *(Why an interactive session could do this when the loop could not: the guard is specific to the settings file under `acceptEdits`, and this session runs under a different permission mode. The two `.claude/` pastes below were still refused — that classifier sits above the permission layer and does not care which session asks.)* Original ask follows. — **one paste, no drafting, and it closes a hole in a rule that says `[ENFORCED]`.** Replace the whole of `tools/loop/loop-settings.json` with the block in `tools/loop/loop-settings-paste.md` (open it and copy the single fenced `json` block; the file also carries the measurement table and the reasoning).\n  What it fixes: `§8.2`'s enforcement half is that file's deny list, and its patterns match whole tokens **from the start of the command**, so they only cover the spelling where the flag comes first. Measured at iter130 against a **nonexistent remote name**, so no probe could reach GitHub or push anything: `git push --force <remote> HEAD` → **denied**; `git push <remote> HEAD --force` → **allowed**; `git push <remote> HEAD -f` → **allowed**; `git push --force-with-lease <remote> HEAD` → **allowed**. With `origin main` in place of that remote, row two would have rewritten `origin/main` and §8.2 says that cannot be undone. No deny list closes it — a remote and a refspec of any name sit between `git push` and the flag, so there is no prefix to match — which is why the fix is a `PreToolUse` hook: `tools/loop/hooks/deny-history-rewrite.py`, committed, inert until this paste wires it, proven both directions 25/25 by `python3 tools/loop/mutate-force-push-guard.py`.\n  Why the loop did not do it: `Edit` on `tools/loop/loop-settings.json` was **refused** under `acceptEdits`, while a `Write` of a new file under `tools/loop/hooks/` succeeded in the same session — the guard is specific to the settings file. Routing around it with an allowlisted `python3` was deliberately not done, on the `paste-rule-8-2a` precedent: this change only *narrows* authority, but anything that can add a `PreToolUse` hook to its own settings can also add one that auto-approves, and that is exactly what the guard is there to stop. `.mtk/paths-130/validate-settings-paste.py` asserts the block adds no `allow` entry, drops none, and keeps every existing `deny` (45 allow / 9 deny, live 45 / 6).\n  **PROVEN WORKING BEFORE YOU INSTALL IT (iter131).** iter130 left the central question open — *does a hook declared in a `--settings` file actually load and fire?* — and answered it \"the iteration after Mirko pastes\". It did not need the paste: `python3 .mtk/paths-131/probe-hook-from-settings.py` hands **the exact fenced block below** to a child `claude -p` session as a throwaway scratch settings file and runs three children, **3/3** — (A) paste settings + `git push deny-probe-nonexistent-remote HEAD --force` → the hook **refused it** with the §8.2 message; (B) control, today's live settings, same command → **reached git** and died on the nonexistent remote, reproducing the hole exactly; (C) paste settings + `git status --short` → **ran clean**, so the guard is selective and not a blanket block. Nothing was installed: the scratch file lives under `.mtk/`, the loop's own settings were never touched, and the probe refuses to run at all if the scratch would add an `allow` entry. So this is a tested fix, not a hopeful one. After you paste, `git push deny-probe-nonexistent-remote HEAD --force` is still worth running once as the in-situ confirmation.\n  **ONE BEHAVIOUR CHANGE SINCE YOU WERE ASKED, ITER163: the hook now FAILS CLOSED on a payload it cannot parse**, where it used to print to stderr and allow. Measured, one child per exit code (`.mtk/paths-163/probe-hook-audibility-as-the-loop-runs-it.py`): in the driver's invocation a hook's stderr reaches nobody at exit 0 **or** exit 1, while exit 2 is quoted back verbatim — so \"warn and continue\" does not exist here. **Worst case for you:** if the CLI's payload shape ever changes, every `Bash` call is refused and the iteration ends BLOCKED rather than running with the guard silently off; recovery is dropping the `hooks` key again. The matcher is `Bash`-only, so the iteration can still write state and tell you. 25/25 unchanged; new cells in `tools/loop/mutate-soft-flags.py` (16/16).\n\n  **Until it is pasted the hole is open**, and what stands in its place is that the loop has never pushed anything in 130 iterations (`origin/main` is still at iter74) plus the flag-first denies, which do work.",
  "paste-format-on-edit-hook": "PENDING, MIRKO'S, ONE PASTE, NOTHING WAITS ON IT. `.claude/settings.json`'s format-on-edit hook has fired and FAILED on every edit since the 2026-07-24 bootstrap (`$CLAUDE_PLUGIN_ROOT` is empty in a project settings file → `bash \"/hooks/format-on-edit.sh\"` → exit 127; the script was never generated). Invisible for 133 iterations because a failing PostToolUse hook never reaches the agent. Hooks are NOT gated by the untrusted workspace — that gates `permissions.allow` only, which closes iter131's lead. Paste tools/loop/claude-settings-paste.md's fenced block over that file, or answer \"delete\" in decisions.formatOnEditHook. Fix committed and tested 5/5, paste validated 12/12, proven end to end 3/3. Full instructions in GATES.md.",
  "settings-corrections": "PENDING, MIRKO'S, TWO ONE-LINERS, NOTHING WAITS ON EITHER. (a) This workspace is untrusted (~/.claude.json hasTrustDialogAccepted:false), so three `.claude/settings.json` allow entries — the `dotnet build/test/format` ones — are ignored every iteration; zero cost today because --settings supplies the broader `Bash(dotnet:*)`. (b) `Write(.env)` and `Write(.env.*)` in your GLOBAL settings enforce nothing: only `Edit(path)` rules are matched for file permissions. Full text in GATES.md.",
  "settled-ci-findings-iter134-135": "SETTLED, MOVED OUT OF GATES.md AT ITER151 as budget payback (the iter140 precedent: nothing discarded, round trip asserted first). These are gate-m6's two closed CI findings. Both were FIXED and re-verified when they were written, and neither changes gate-m6's steps - which is why they are here and the steps are still there. Verbatim, in the order they appeared:\n\n**AND READ THIS ONE TOO — iter134. THE PUSH IN STEP 2 WOULD HAVE TURNED `ci.yml` RED, AND IT NO LONGER WILL.** `ci.yml` triggers on push to `main`, and until commit `f04e367` nothing had ever run it anywhere but a developer's working tree. Run verbatim against a clean clone with the runner's own environment (no secrets, `CI=true`, `GITHUB_ACTIONS=true`), its **Test step exited 2 — 1 failed of 1370**, where the same suite is green locally. The clean clone was not the cause: isolated over `{clone, live tree} × {no markers, CI, GITHUB_ACTIONS, both}`, **`GITHUB_ACTIONS=true` alone fails and `CI=true` alone does not** — Spectre.Console colourises when it detects a CI host, and one assertion's plain substring straddled a colour boundary (47 escape sequences in the child's stdout, 0 in every passing cell). Fixed in the test harness rather than the CLI, because colour in a runner's log is worth keeping: `NO_COLOR=1` now sits beside the credential placeholders `DocumeCli` already pins for the same \"passes locally only\" reason, with a host-independent regression test. **Re-verified at the new HEAD across two clone shapes — a full clone and a `--depth 1` clone, which is what `actions/checkout@v4` actually produces — 12/12 steps green**, including `tools/verify-analyzers.sh` and both `claude plugin validate --strict` calls. Re-runnable in ~90 s: `python3 .mtk/paths-134/probe-ci-clean-clone.py`. *(Not a new dependency for you, and nothing here changes the steps below — it removes a surprise that was waiting at step 2.)*\n\n**One thing that run also measured — and iter135 has since closed it.** On a clean clone **7 tests skipped**, and they were exactly the 7 that drive the real mermaid renderer: `node_modules/` is gitignored and `ci.yml`'s `build-test` job installed no Node and ran no `npm ci`, so the renderer was never exercised in CI — the same blind spot as the open `decisions.mermaidDialectGap`, in the one place that could have caught it moving. The job now runs `actions/setup-node@v4` (Node 22) and `npm ci` before the suite, measured on a `--depth 1` clone with the runner's environment: **without those steps 1368 passed / 7 skipped and the job was still green; with them 1375 / 1375 and nothing skipped** (`python3 .mtk/paths-135/probe-ci-renderer-coverage.py`, 5/5). ~1 s to install 3 packages, no browser. *(Nothing here changes the steps below.)*",
  "settled-notification-correction-iter139": "*(Corrected iter139: the second half of this line used to read \"Each gate also fires a macOS notification when opened\", and **no such notification exists.** `docume-loop.sh` has exactly six `notify()` call sites — loop start, STOP file, usage limit, DONE, BLOCKED, and WAITING-GATE — and none is tied to a gate being **created**. What actually fires is the WAITING-GATE one, \"Waiting on a human gate — see GATES.md\", identical every ~30 minutes whether or not a gate is new, 19 times so far; an iteration that opens a gate and still has other work ends CONTINUE and notifies nothing at all. The `osascript` call does exit 0, but Notification Center's DB is TCC-blocked to the loop, so nobody can prove from in here that it reached you.)*"
}
