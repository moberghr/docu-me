NEXT INCREMENT = M2 SLICE 6, `docume publish --dry-run` END TO END, STILL OFFLINE. This is the first slice that puts a publish PIPELINE in front of the pieces slices 1-5 built, and it is verifiable in a headless iteration because it needs no credentials, no sandbox space and no network: it decides and prints, it writes nothing. Read "(N) SHIPPED AT ITER31" below first — the banner is done and its injection seam is what the pipeline composes.

WHAT IT WIRES, IN §6.2's ORDER (every part already exists and is tested; this slice is composition, not new algorithms):
 1. ConfigLoader -> DocumeConfig (docume.json, §5.1); StateStore.Load -> DocumeState (_meta/state.json, §5.3).
 2. WikiTree over the configured wiki path -> pages + titles + parent relationships; build the PageLinkResolver / AttachmentResolver / MermaidDiagramResolver trio (src/DocuMe.Core/Markdown/PageResolvers.cs) for the WHOLE tree BEFORE converting any page (§6.2 step 2: the link map is tree-wide, which is why it cannot be built per page).
 3. Per page: ConfluenceStorageConverter.Convert(...) -> storage format; ContentHash.OfBody(body) -> the hash; ContentHash.OfBytes over each attachment's bytes.
 4. PublishPlanner.PlanPage(...) -> Create | Update | UpdateAttachments | Skip plus InvalidatesApproval; PublishPlanner.OrphanPages(state, presentPaths) once for the run.
 5. PRINT the summary with Spectre.Console: created / updated / attachments-only / skipped / orphans, and which approvals a real run would invalidate. NOTHING uploaded, NOTHING written to state.json.
 6. The banner belongs in the printed plan, not only in slice 7: `new PageBanner { BaselineSha = state.BaselineSha, GeneratedOn = <run date>, DashboardTitle = config.Dashboard.Title }.InjectInto(body)` is what a real publish would upload. THE RUN DATE IS THE CALLER'S DECISION and PageBanner deliberately refuses to make it: use ONE DateOnly for the whole run (`DateOnly.FromDateTime(DateTime.UtcNow)` — UTC, so a laptop and a CI runner in different zones produce the same banner), computed once in the command, never per page.

WHERE THE CODE GOES: src/DocuMe.Cli/Commands/PublishCommand.cs, alongside the existing ConvertCommand.cs and InitCommand.cs, registered in Program.cs the way they are. The DECISION and REPORTING logic worth testing must NOT live in the command — put it in src/DocuMe.Core (a PublishPipeline / PublishReport pair) so tests drive it without System.CommandLine, exactly as ConvertCommand keeps its work in Core.

HARD REQUIREMENT, MUST LAND IN THIS SLICE (§0.1, §1.4): THE PRODUCTION GUARD. Nothing in ConfluenceClient knows which space it writes to. The guard belongs in the publish path: REFUSE when the resolved target space key equals confluence.productionSpaceKey ("AUR") unless confluence.productionAllowed is true, and make it a TEST, not a comment. --dry-run writes nothing so it is safe by construction, but the guard must exist in the pipeline BEFORE slice 7 wires a real write, not after. Second constraint from the same direction: state.json -> confluence.sandboxSpaceKey is STILL null, so a dry-run must NOT require a space key to be resolvable at all — decide and print without one, and fail loud only where a write would actually happen.

THEN SLICE 7 wires the real writes to ConfluenceClient (create / update / upload / labels), the state write-back (StateUpdates.RecordPublish / InvalidateApproval / RecordLastPublishedSha) and --force / --changed-since / --prune. It is the first slice that genuinely needs the sandbox setup in GATES.md done (space key, base URL, cross-tenant token, loop relaunched from a shell that exports the credentials).

=== (N) SHIPPED AT ITER31 — THE §8 BANNER, M2 SLICE 5. Read before touching src/DocuMe.Core/Markdown/PageBanner.cs ===
Commit 63fbac8. NEW ONLY (nothing modified, no new packages, nothing on the network): src/DocuMe.Core/Markdown/PageBanner.cs, tests/DocuMe.Core.Tests/Markdown/PageBannerTests.cs (15 tests). 495 tests (was 480), build 0 warnings.
WHAT IT IS: a pure `sealed record PageBanner { string? BaselineSha; DateOnly? GeneratedOn; string? DashboardTitle }` with two methods — `Render()` returning the storage-format fragment, and `InjectInto(body)` returning banner + "\n" + body. No clock, no state, no config dependency: the three inputs are handed in.
THE EXACT OUTPUT, PINNED AS A LITERAL (PageBannerTests.Render_PinsThePanelAndTheText). A human reviews this on all 79 pages at gate-m2-aur-review, so treat a wording change as a review item, not a refactor:
  <ac:structured-macro ac:name="info"><ac:parameter ac:name="icon">true</ac:parameter><ac:rich-text-body>\n<p>Generated by DocuMe from commit SHA on 2026-07-25. Edit the source in the repository; changes made on this page are overwritten by the next publish.</p>\n<p>Review status is shown by page labels and the <ac:link><ri:page ri:content-title="Documentation Status"/><ac:link-body>Documentation Status</ac:link-body></ac:link> page.</p>\n</ac:rich-text-body></ac:structured-macro>
THE ONE CONTENT DECISION BEYOND §8's LITERAL TEXT: §8 asks for provenance plus the review-status sentence; the "Edit the source in the repository; changes made on this page are overwritten by the next publish." clause is an addition, justified by §9.1 (the repo is the source of truth, hand edits are lost on republish) and placed where the person about to make that edit is looking. It is one string and one test, cheap to drop if the review gate rejects it. `icon=true` mirrors what ConfluenceStorageRenderer already writes on GitHub alert panels rather than trusting a server-side default a site could change.
WHY IT IS NOT IN THE CONVERTER, AND WHY THAT IS THE WHOLE POINT: ConfluenceStorageConverter.Convert's output IS the ContentHash.OfBody preimage. A banner emitted from inside it would put a per-publish DATE into the hash, so every page would look changed on every run and §8/§9.2 approval would be revoked wiki-wide, silently. Injection is therefore a separate seam used AFTER the hash is computed (§6.2 step 5's order). PageBannerTests.Publish(...) encodes that order as a two-line helper and three tests pin it: two banners differing only by date upload DIFFERENT bodies and store the SAME hash; the stored hash equals OfBody(pre-banner body) and NOT OfBody(upload); InjectInto does not mutate its input.
DEGRADATIONS, ALL TESTED, ALL "OMIT RATHER THAN PRINT AN EMPTY CLAUSE": null-or-empty BaselineSha drops "from commit " (what a publish before the first generation run looks like — §5.3 types baselineSha nullable); null GeneratedOn drops the date; null-or-empty DashboardTitle makes the sentence "Review status is shown by page labels." with NO ac:link, because a page link to a title that does not exist renders broken on all 79 pages.
ESCAPING GOES THROUGH ConfluenceStorageRenderer (WriteEscaped / WriteAttributeEscaped), by constructing one over a StringWriter purely for those two methods: a second escaping implementation is a second thing to get wrong, and BOTH inputs are consumer-supplied (state.json baselineSha, docume.json dashboard.title). A title containing & " < is pinned in BOTH the attribute and the link body. Do not "simplify" this to string interpolation.
DATE FORMAT IS yyyy-MM-dd WITH EXPLICIT InvariantCulture, and that is NOT covered by a test: a culture-swap test would have to mutate CultureInfo.CurrentCulture inside a parallel xUnit v3 run, so the explicit InvariantCulture in the source is the only guard. A regression there shows up as a locale-dependent banner, not a failing test.
A WELL-FORMEDNESS TEST SITS BESIDE THE PINNED LITERAL: XDocument.Parse over the fragment wrapped in a root that declares the ac/ri prefixes (test helper `Wrap`). The literal was hand-written, so an unbalanced tag would be pinned by an equally unbalanced expectation and then rejected by Confluence at upload time on every page of the run. Reusable by any future storage-format producer (the dashboard page, §6.5).
NO CALL SITE EXISTS YET, ON PURPOSE: publish is the command that will have one (slice 6). Nothing in the CLI references PageBanner.

REPO HYGIENE OBSERVED AT ITER31 — neither item is this slice's to fix, both are worth knowing:
`dotnet format --verify-no-changes` reports 56 findings repo-wide (WHITESPACE, RCS1139/SA1604 missing doc summaries, SA1642, SA1618) in files this slice never touched, a PRE-EXISTING condition from before the analyzer pass, while `dotnet build` is 0 warnings. PageBanner.cs and PageBannerTests.cs contribute ZERO of them (verified by grep). That belongs to the standards-hardening work, not to a feature slice.
Two untracked strays, neither committed: `tests/golden/.claude/analytics.json` (an MTK analytics file written by a tool that ran with cwd=tests/golden — harmless, the golden corpus enumerates *.md only) and `tools/loop/MODEL` (contains "claude-opus-5"; loop harness, someone else's).

=== (M) SHIPPED AT ITER30 — contentHash + THE PUBLISH DECISION, M2 SLICE 4. Read before touching src/DocuMe.Core/State/ ===
Commit ebbae1f. 480 tests (was 435), build 0 warnings, NO new packages, nothing touching the network. THE §5.3 STATE MODEL ITSELF ALREADY EXISTED — DocumeState/PageState/ApprovalState/ApprovalHistoryEntry + StateStore (load with version-migration seam, save) shipped at M0 in commit 8b8b4c1 and matched §5.3 field for field, so slice 4 was NOT "write the state file"; it was the computation layer §6.2 steps 5, 7 and 8 need on top of it. A future iteration that re-reads the handoff should not go looking for a missing model.
NEW FILES: src/DocuMe.Core/State/ContentHash.cs, PublishPlan.cs (PagePublishAction + PagePublishPlan + PublishPlanner), StateUpdates.cs (PublishedPage + StateUpdates). MODIFIED: DocumeState.cs (+ApprovalStatus constants "approved"/"needs-review"). TESTS: ContentHashTests (12), PublishPlannerTests (18), StateUpdatesTests (15).
THE HASH CONTRACT, NOW PINNED — DO NOT CHANGE ANY CLAUSE WITHOUT TREATING IT AS A MIGRATION: preimage = UTF-8 bytes of the converter's storage-format output, after \r\n and \r collapse to \n and the ends are Trim()ed (same normalization MermaidAttachmentName uses, so a Windows checkout hashes like CI); spelling = "sha256:" + 64 LOWERCASE hex, per §5.3's own example. Pinned in tests against constants computed OUTSIDE this code (node crypto), including a non-ASCII case, so the test cannot drift with the implementation. Attachment hashes are the same spelling over VERBATIM bytes — no normalization, because 0x0D 0x0A inside a PNG is data, not a line ending. ContentHash.OfBytes takes a ReadOnlySpan<byte>, which the client's ReadOnlyMemory<byte> upload payload satisfies via .Span.
THE DECISION TABLE PublishPlanner.PlanPage IMPLEMENTS (pure; no IO, no clock — which is what lets --dry-run and the real run share one decision): no state entry OR entry without pageId (the `init --adopt` skeleton) -> Create, every attachment uploaded. Body hash moved OR --force -> Update. Body identical but some attachment's BYTES moved -> UpdateAttachments. Otherwise -> Skip. plan.WritesBody is true for Create/Update only.
THE ONE DELIBERATE DEVIATION FROM §6.2's LITERAL TEXT (recorded as spec finding 9 below): step 5 reads "unchanged vs state and not --force -> skip", but contentHash covers the BODY only. A hand-placed image (docs/wiki/img/foo.png) keeps its filename when its bytes change, so a literal skip would leave the published page showing last month's picture. Hence UpdateAttachments: upload the changed bytes, do not consume a page version, do not touch approval. Mermaid diagrams cannot reach that path — MermaidAttachmentName derives the filename from the source, so a changed diagram changes the body too.
APPROVAL INVALIDATION IS KEYED EXACTLY AS §8 REQUIRES: plan.InvalidatesApproval is true ONLY when the body hash moved AND the page's approval status is "approved". Attachment-only changes do not invalidate, and --force on an unchanged page does not invalidate (unchanged hash = unchanged content; --force distrusts the REMOTE, not the source, which is why it also re-uploads every attachment). Both are tests, not comments.
StateUpdates IS PURE TRANSITIONS, DELIBERATELY SEPARATE FROM StateStore's FILE IO: RecordPublish overwrites the six fields publish owns (pageId, title, parentPageId, contentHash, publishedVersion, attachments — the FULL attachment set, not the uploaded subset) and carries approval, stale and feedbackCursor through untouched, because those belong to sync (§6.3) and drift (§6.4). InvalidateApproval moves an approved page to needs-review, nulls approvedBy/At/Version and APPENDS the withdrawn approval to history (§8 keeps history for audit; a financial org reads that as never-rewritten) — it is total and idempotent, so the pipeline can call it whenever plan.InvalidatesApproval is true without re-deriving anything. Also RecordLastPublishedSha and RemovePage (the state half of a confirmed --prune, §9.6). Every method returns a NEW state; a test asserts the input is not mutated.
ORPHANS: PublishPlanner.OrphanPages(state, presentPaths) returns state entries whose file is gone, sorted ordinally — reported by every run, deleted only by --prune after interactive confirmation (§9.6). PagePublishPlan.OrphanAttachments is the per-page analogue and is REPORT-ONLY on purpose: §6.2 gives attachment removal no verb, and an unreferenced attachment is invisible on the page.
ANALYZER NOTES FOR THE NEXT SLICE (all three cost a build cycle here): CA1859 is an ERROR in this repo, so a private helper returning IReadOnlyList<string> from a .ToArray() must be declared string[]; an array's .Count is an explicit interface member so it becomes .Length; SA1122 rejects "" in favor of string.Empty even in tests.

=== (L) SHIPPED AT ITER29 — ATTACHMENTS AND LABELS, M2 SLICE 3. Read before touching ConfluenceClient ===
Commit 818fd1d. MODIFIED ONLY (no new files, no new packages): ConfluenceClient.cs (+UploadAttachmentAsync, +AddLabelsAsync, +RemoveLabelAsync, transport generalized from "string payload" to HttpContent + an ApiSurface enum), ConfluenceModels.cs (+ConfluenceAttachmentUpload, +ConfluenceAttachment, +ConfluenceLabel), ConfluenceRequests.cs (+LabelCreate), ConfluenceResponses.cs (+ContentBulk, +LabelBulk, and a remark that MultiEntityResult<T> is now shared with v1), ConfluenceClientTests.cs (+14 methods / +15 cases). 435 tests (was 420), build 0 warnings. THE CONFLUENCE CLIENT IS NOW FEATURE-COMPLETE FOR M2 — nothing in the CLI wires any of it yet, on purpose: publish is the command that will.
THE v1 SWAGGER IS FETCHABLE THE SAME WAY THE v2 DOCUMENT IS, and it is the source of truth for anything else v1: https://dac-static.atlassian.com/cloud/confluence/swagger.v3.json (414 KB, 200 OK, content-type text/plain). Same node recipe as iter27. Its servers[0].url is '//your-domain.atlassian.net' and EVERY path is spelled with the /wiki prefix already ('/wiki/rest/api/content/{id}/label'), unlike the v2 document whose server URL carries /wiki and whose paths do not. Do not read a leading /wiki out of a v1 path and double it.
v2 REALLY HAS NO WRITE FOR EITHER (re-confirmed): that is why these are v1. §4's "v1 only where v2 lacks" now has exactly one instance, and the class doc says so.
PUT, NOT POST, AND THIS IS THE DECISION OF THE SLICE: v1 offers BOTH verbs on /rest/api/content/{id}/child/attachment and they are NOT interchangeable. POST is "Create attachment" and its OWN documented 400 is "Returned if the content already has an attachment with the same filename". PUT is "Create or update attachment", which stores a NEW VERSION of an existing name. §6.2 uploads ONLY the attachments whose hash changed, so BY CONSTRUCTION almost every upload DocuMe ever performs is to a name that already exists — POST would fail exactly the uploads that matter and succeed only on a first publish. Pinned by Uploads_with_the_verb_that_replaces_an_existing_name_rather_than_the_one_that_rejects_it, which stubs BOTH verbs the way the real API answers (POST -> 400 "same file name as an existing attachment", PUT -> 200), so a future refactor that switches verbs fails loudly instead of silently.
WHAT THE UPSERT DOES NOT GIVE YOU: there is no version to send, so there is NO OPTIMISTIC LOCK on an attachment — the ConfluenceConflictException story has no attachment equivalent. Attachments are machine-owned so nothing should be racing, but nothing would report it if something were. Written into the XML doc, not just here.
MULTIPART REPLAY IS PROVED, NOT ASSUMED — this was the open question the iter28 handoff flagged: A_rate_limited_upload_is_replayed_with_the_same_bytes PASSES. MultipartFormDataContent built from ByteArrayContent + StringContent parts is re-serializable, so the resilience handler re-sends the identical body after a 429 (test asserts request #2 still carries both the SVG and the filename). A StreamContent part would be drained by attempt 1 and upload an EMPTY FILE OVER A WORKING ATTACHMENT on attempt 2 — silent corruption, not an error. THAT is why ConfluenceAttachmentUpload.Content is ReadOnlyMemory<byte> and not a Stream, and the XML doc says so. Do not "optimize" it to a stream.
THE EXACT MULTIPART BYTES ON THE WIRE, DUMPED RATHER THAN GUESSED (probe method, 13 slices unbeaten — the first assertions were wrong and the dump corrected them in one cycle):
  Content-Disposition: form-data; name=file; filename=docs_architecture_data_flow.svg; filename*=utf-8''docs_architecture_data_flow.svg
.NET writes the part name as a BARE TOKEN (name=file, NOT name="file") and emits the file name TWICE — plain filename= plus the RFC 5987 filename*=. Assert the unquoted spellings. The duplication is new sandbox item (16).
THE FILE PART CARRIES ITS OWN Content-Type from upload.ContentType and that is LOAD-BEARING, not cosmetic: Confluence records it as the attachment's media type, which is what decides whether an <ac:image> renders inline or degrades to a download link (sandbox item (8)). It has no default — the caller must state it — so the publish slice has to map extension -> media type deliberately.
minorEdit IS REQUIRED BY THE SCHEMA and is always 'true': an ~80-page publish uploading 59 rendered diagrams would otherwise raise 59 notifications and activity-stream entries, which is exactly the machine churn the sandbox space exists to keep off people's feeds. comment is optional and OMITTED as a whole part when null (same null-omission rule as the page write; two tests pin both directions).
AN EMPTY ATTACHMENT NEVER REACHES THE WIRE (test asserts LogEntries.Count == 0). Unlike a page body — where empty is legitimate and RequireBody says so — zero bytes has no legitimate meaning here: DocuMe only uploads rendered diagrams and repo images, so it means the producer failed, and the upload would NOT error, it would store a new version of a working attachment containing nothing.
LABELS: POST /rest/api/content/{id}/label with an ARRAY body of {prefix, name} (both required by the v1 LabelCreate schema; the endpoint also accepts a bare object, but an array is the shape that does not change when a caller adds a second label). Prefix is 'global' — the namespace an ordinary human-visible label lives under. v1 documents "Does not modify the existing labels", so re-adding an existing label is NOT an error, which is what makes a re-run of 'drift --mark' safe.
LABEL REMOVAL IS INCLUDED (§8 approval invalidation needs it and it is the same seam): DELETE /rest/api/content/{id}/label?name=<name>, 204 with an EMPTY BODY, so it is the one path that does not deserialize. The ?name= spelling rather than the /label/{label} path segment is deliberate — Atlassian recommends it for names containing '/', and DocuMe's label names are consumer-configurable (docume.json -> labels, §5.1), so this client does not get to assume them. A test pins that a name with a space and an '&' survives (assert Query["name"].Single(), never the raw URL — RequestMessage.Url is already decoded).
X-Atlassian-Token: nocheck ON BOTH v1 WRITES, and note the spelling is 'nocheck', NOT the 'no-check' the iter28 handoff guessed. v1 documents it as MANDATORY for the multipart upload (which accepts multipart/form-data and is otherwise blocked as suspected XSRF). The LABEL write does not document needing it; it is sent there anyway, and the reasoning is written in the class doc: the verify-then-emit rule (iter23's brush precedent, iter26's ac:width deferral) governs OUTPUT — a guessed attribute becomes a wrong artifact and §8 hash drift — whereas an inert request header produces no artifact at all, so the cost asymmetry runs the other way. If a sandbox run ever shows it matters, that is a fact, not a rollback.
THE SECOND SEAM WAS INDEED SMALLER THAN IT LOOKED, exactly as the iter28 note predicted: the base address is <site>/wiki/ and a v1 call is just the relative 'rest/api/…' off it. Pinned by Composes_the_v1_path_off_the_same_base_as_the_v2_calls, a [Theory] over trailing-slash/no-trailing-slash mirroring the v2 one, because that bug class silently relocated every request once already.
THE TRANSPORT CHANGE, so the next slice does not re-derive it: SendAsync went from (method, path, string? payload) to (method, path, HttpContent? content, ApiSurface surface). WriteAsync still serializes JSON and now delegates to a new SendContentAsync, which is also what the multipart path uses. ApiSurface is a PRIVATE NESTED ENUM {V2, V1} passed EXPLICITLY at every call site rather than inferred from the path string, because "which requests are v1" should be stated, not parsed. Content ownership: the caller wraps its HttpContent in a 'using' (CA2000) and HttpRequestMessage.Dispose also disposes it — double dispose of HttpContent is idempotent, this is fine.
NEW TEST-SIDE HELPERS worth reusing: LegacyPath(endpoint) -> "/wiki/rest/api/content/{PageId}/{endpoint}", with AttachmentPath and LabelPath on top; MultipartBody(server, index) decodes IRequestMessage.BodyAsBytes as UTF-8 (BodyAsBytes is what WireMock populates for a multipart body — Body/Payload are for JSON). ApprovedLabel is a static readonly string[] because CA1861 (severity=warning, and warnings are errors) REJECTS an inline array literal as an argument.

=== (K) SHIPPED AT ITER28 — THE WRITE PATHS, M2 SLICE 2. Read before touching ConfluenceClient ===
Commit 40ecc79. NEW: src/DocuMe.Core/Confluence/ConfluenceRequests.cs (internal wire shapes for the writes). MODIFIED: ConfluenceClient.cs (CreatePageAsync + UpdatePageAsync + the transport generalized from GET-only to method+payload), ConfluenceExceptions.cs (+ConfluenceConflictException, + an optional 'operation' on ConfluenceApiException), ConfluenceModels.cs (+ConfluencePageDraft, +ConfluencePageRevision), tests/DocuMe.Core.Tests/Confluence/ConfluenceClientTests.cs (+9 cases). 420 tests (was 411), build 0 warnings. NOTHING in the CLI wires it yet, on purpose: publish is the command that will.
SHAPES READ FROM THE OPENAPI DOCUMENT, NOT MEMORY (same recipe and URL as slice 1). POST /api/v2/pages: requestBody PageCreateRequest requires ONLY spaceId; status/title/parentId/body are optional; query params embedded/private/root-level exist and are unused. It answers 200 (NOT 201) with PageSingle. Documented failures are 400/401/404/413, where 413 is a body over 5 MB. PUT /api/v2/pages/{id}: PageUpdateRequest requires id, status, title, body AND version; answers 200; documented failures 400/401/404. The path id is typed integer int64 while the BODY id is a string — both are sent as the string DocuMe holds.
TWO NARROWINGS FROM THE SCHEMA'S OWN NOTES: spaceId is NOT sent on an update, because the schema says it "currently does not support moving the page to a different space", so it could only restate what Confluence knows; parentId IS sent, because it is how a page moves within a space when the wiki tree is reorganized.
NULLS ARE OMITTED, NOT SENT: WriteSerializerOptions = web defaults + JsonIgnoreCondition.WhenWritingNull. This is load-bearing twice. An ABSENT parentId is documented ("the space homepage's ID will be used"); a '"parentId": null' is a value the endpoint documents no handling for. And an absent version.message leaves the history line as Confluence writes it. A test asserts the field is missing, not null.
THE +1 LIVES IN THE CLIENT: ConfluencePageRevision carries CurrentVersion (the version as READ) and UpdatePageAsync sends CurrentVersion + 1, so no caller ever does the arithmetic. A CurrentVersion below 1 throws before anything reaches the wire (test asserts server.LogEntries.Count == 0), because version 1 over an existing page is how a publish would blank someone's work.
THE 409 DECISION, WHICH THE NEXT SLICE MUST NOT QUIETLY REVERSE: a version conflict is REPORTED, NEVER RESOLVED. No retry, no re-read-and-push. Re-reading the version and pushing again would always succeed — the repo is the source of truth and republish overwrites hand edits by design (§9.1) — and that is exactly its cost: it would erase the only signal DocuMe gets that a human was editing, and would make a race between two publish runs interleave two different repo states silently. The failed page is reported once; the re-run reads the current version and republishes. The reasoning is written out in the XML doc on ConfluenceConflictException, not just here.
409 IS NOT IN THE OPENAPI DOCUMENT AT ALL — probed: ZERO of its operations document a 409, and PUT /pages/{id} lists only 400/401/404 — yet Atlassian's own developer community documents the API answering 409 "Version must be incremented when updating a page". So BOTH paths are handled: a 409 becomes ConfluenceConflictException, and any other non-2xx becomes ConfluenceApiException that names the page and the version it tried and quotes Confluence's words. New sandbox item (14) settles which branch is live code.
RETRY NOW APPLIES TO WRITES, AND THE CLASS DOC DEFENDS IT: a 429 is a REJECTION (Confluence never applied the request), so replaying is exactly right and §13 S5 expects a bulk publish to lean on it. A 5xx replay of a create is the ambiguous one, and what bounds it is Confluence's per-space TITLE UNIQUENESS (the constraint §6.2 step 1 already validates against): a replayed create whose first attempt actually applied comes back as a loud duplicate title, not a silent second page. 409 and 400 are never retried — the hand-written predicate handles 408/429/5xx only.
StringContent IS CHOSEN FOR REPLAYABILITY, not convenience: it buffers, so the resilience handler can re-send the same HttpRequestMessage. A streamed body would be consumed by attempt 1 and silently send NOTHING on attempt 2. Pinned by A_rate_limited_create_is_replayed_with_the_same_body, which asserts the SECOND request still carries the body. CHECK THIS AGAIN FOR MULTIPART in slice 3.
ConfluenceApiException.Operation EXISTS BECAUSE CONFLUENCE'S ERRORS ARE FLAT PROSE: a duplicate title, an oversized body and an invisible parent all arrive as a bare 400 or 404, and the request path for a create is just 'api/v2/pages'. The message now reads "… while creating page 'Costs & billing' in space 98304: '<excerpt>'". A read passes no operation (its path already names the space or page id).
THE CLASS DOC'S READ-ONLY SENTENCE IS GONE, as the previous handoff required — it now describes the upsert and the retry-safety argument instead. Any future slice that adds a write must keep that paragraph honest.
THE NINE TESTS AND WHAT EACH PINS: create returns the assigned id + version 1 and sends spaceId/status=current/title/parentId/body.storage{representation,value}; a parentless draft OMITS the field; an update sends version+1, the parent, NO spaceId and NO null version message; a version message is sent when supplied; a 409 names the page, says "version 8", quotes Confluence and logs exactly ONE request; a rejected create carries the title it tried (Operation) and the body's "already exists", one request; a rate-limited create replays with the same body; a 401 on a PUT stops dead and leaks no token; a version below 1 never reaches the wire.
WIREMOCK FACT WORTH NOT REDISCOVERING: IRequestMessage.Body IS populated for an application/json request body, so JsonDocument.Parse over it works. The suite funnels that through Payload(server, index = 0), which CLONES the root element so it outlives the JsonDocument's using scope — assert on the parsed payload, never the raw string, or property order and whitespace become part of the test.

=== (J) SHIPPED AT ITER27 — THE CONFLUENCE CLIENT, M2 SLICE 1. Read before touching src/DocuMe.Core/Confluence/ ===
Commit 6920cab. NEW: src/DocuMe.Core/Confluence/{ConfluenceClient,ConfluenceHttp,ConfluenceCredentials,ConfluenceClientOptions,ConfluenceModels,ConfluenceResponses,ConfluenceExceptions}.cs and tests/DocuMe.Core.Tests/Confluence/{ConfluenceClientTests,ConfluenceCredentialsTests}.cs. MODIFIED: Directory.Packages.props, both csproj. 411 tests (was 388), build 0 warnings. NOTHING in the CLI wires it yet, on purpose: publish is the command that will.
SPIKE S1 IS RESOLVED, WITH EVIDENCE RATHER THAN THE DEFAULT: Dapplo.Confluence is alive (1.0.41, published 2026-01-27, has a net10.0 target) but src/Dapplo.Confluence/ConfluenceClient.cs sets ConfluenceApiUri = confluenceUri.AppendSegments("rest", "api") — the v1 content API, with no v2 surface — and reaches Atlassian through Dapplo.HttpExtensions + Dapplo.HttpExtensions.JsonNet (Newtonsoft). §4 asks for v2 plus Microsoft.Extensions.Http.Resilience on the transport, so the package would have to be worked around on both counts for ~12 endpoints. Thin custom client, as §13's default said, now for a checked reason. DO NOT RE-OPEN without new evidence about Dapplo shipping v2.
THE OPENAPI DOCUMENT IS FETCHABLE FROM THE HEADLESS LOOP AND IS THE SOURCE OF TRUTH FOR EVERY REMAINING CONFLUENCE SLICE: https://dac-static.atlassian.com/cloud/confluence/openapi-v2.v3.json (611 KB of JSON, 200 OK, content-type text/plain). Recipe that worked: fetch it with node, write it to /tmp, then print paths['/pages'].get.parameters and components.schemas.PageBulk instead of guessing. It confirmed servers[0].url = https://{domain}/wiki/api/v2, the query params (/spaces: keys/ids/limit/cursor; /pages: space-id/title/status/body-format/limit/cursor), the {results, _links} envelope, and that PageBulk.id, PageBulk.spaceId and SpaceBulk.id are all typed string (so no string-or-number id converter was written — if one is ever needed, that decision has evidence behind it). PAGINATION IS BY OPAQUE CURSOR returned in the Link response header; the read paths use limit=1 lookups and do NOT page, so the first listing slice owns that.
WHAT THE CLIENT DOES: FindSpaceByKeyAsync (GET /api/v2/spaces?keys=), FindPageByTitleAsync (GET /api/v2/pages?space-id=&title=&status=current[&body-format=storage]), FindPageByIdAsync (GET /api/v2/pages/{id}, 404 -> null because a state.json page a human deleted is the §6.2 orphan case). All three return null for absent; every failure is an exception.
FOUR EXCEPTION TYPES, and the split is load-bearing: ConfluenceException (abstract base) / ConfluenceCredentialsException (env vars missing) / ConfluenceAuthenticationException (401+403 ONLY — the one a bulk run must stop on, not report per page) / ConfluenceApiException (any other non-2xx, incl. a 429 or 5xx that outlived its retries) / ConfluenceProtocolException (unparseable JSON, no 'results', an entity missing id/title/spaceId/version.number).
THE RETRY RULES LIVE IN THE HANDLER PIPELINE (ConfluenceHttp), NOT IN THE CLIENT, so the tests exercise the real thing: ResiliencePipelineBuilder<HttpResponseMessage>.AddRetry(new HttpRetryStrategyOptions{ ShouldRetryAfterHeader = true, ShouldHandle = IsRetryable }) wrapped in a ResilienceHandler over a SocketsHttpHandler. IsRetryable is HAND-WRITTEN rather than HttpClientResiliencePredicates.IsTransient on purpose: the rule that 401/403 never retry (§1.2) then cannot be widened by a package upgrade. Retryable = HttpRequestException, 408, 429, >=500. MEASURED: a 429 carrying Retry-After: 1 waits 1.011s while the configured backoff is 1ms, so the header genuinely wins.
THREE NARROWINGS THE TESTS PIN, each protecting against a different silent wrong-page bug: status=current is explicit (the endpoint otherwise defaults to current,archived and an archived namesake would shadow the live page); THE RETURNED TITLE IS RE-CHECKED against the requested one, because Atlassian documents the title filter only as "filter the results to pages based on their title" — if it matches loosely, a near-miss reads as "no such page" and ends in a create that Confluence rejects for a duplicate title, never in DocuMe overwriting somebody else's page (new sandbox item (13) is how to find out which it is); and a 200 without a 'results' array FAILS LOUD instead of reading as absent.
ConfluenceCredentials IS DELIBERATELY NOT A RECORD AND HAS NO TOKEN PROPERTY: a generated ToString prints every member, so the token would land in the first log line, exception message or debugger view that formats the object (§0.3, §1.1). It leaves the type only as BasicAuthParameter (base64 email:token), ToString redacts, and FromEnvironment takes an optional Func<string,string?> so tests never touch the process environment. A test asserts a 401 message contains DOCUME_CONFLUENCE_TOKEN and NOT the token value.
BaseUrl NORMALIZATION IS NOT COSMETIC: humans write https://site.atlassian.net/wiki with no trailing slash, and Uri composition would treat 'wiki' as a file name and drop it, silently moving every call to /api/v2. ConfluenceHttp adds the slash; a [Theory] proves both spellings hit /wiki/api/v2/spaces.
WIREMOCK.NET FACTS WORTH NOT REDISCOVERING (2.13.0): scenarios work as documented — the FIRST mapping carries .InScenario(s).WillSetStateTo(next) and no WhenStateIs, and it matches only in the initial state, so "500 then 200" and "429 then 200" are two mappings. RequestMessage.Url IS ALREADY DECODED, so asserting a %20 there tests WireMock, not the wire — assert RequestMessage.Query!["title"].Single() instead (a test with '&' in the title proves the escaping). RequestMessage IS NULLABLE (CS8602) — the suite funnels it through one LastRequest(server) helper. server.LogEntries has .Count (CA1829/CA1826 refuse .Count()), and request counting is what proves 'no retry happened'.
NEW PINS: Microsoft.Extensions.Http.Resilience 10.8.0 (Core), WireMock.Net 2.13.0 (tests only). Both went into Directory.Packages.props (CPM, no inline Version=); Polly comes transitively and CentralPackageTransitivePinningEnabled handles it, no extra PackageVersion needed.

=== (I) SHIPPED AT ITER26 — THE MERMAID RENDER PASS. Read before touching diagram code ===
Commit be9956a. NEW: src/DocuMe.Core/Acceptance/{MermaidAcceptance,DiagramRenderReport,QuotedTokens}.cs, tests/DocuMe.Core.Tests/Acceptance/MermaidAcceptanceTests.cs, tests/DocuMe.Core.Tests/Markdown/BundledRenderScript.cs. MODIFIED: AcceptanceReport, ConversionAcceptance, MermaidRenderer, ConvertCommand, Program, two test suites. 388 tests (was 379), build 0 warnings.
CLI: 'docume convert <wiki-root> --render-mermaid [--renderer <path>]'. --renderer defaults to 'tools/render-mermaid.mjs' (where docume init scaffolds it and docume.json -> mermaid.renderer names it); convert does NOT read docume.json yet, so the (D) run must pass '--renderer templates/tools/render-mermaid.mjs' explicitly. Program.Main is now async and uses InvokeAsync (a sync Invoke cannot drive an async action).
SPEC FINDING (9) IS NOW SETTLED IN MECHANISM AND HALF-ANSWERED IN FACT: over tests/golden the real beautiful-mermaid 1.1.3 REFUSES 2 OF 4 DIAGRAMS — 'graph TD;' (trailing semicolon on the header, which mermaid.js and GitHub both accept) and 'pie' — while conversion reports ZERO failures on those same pages. Pinned by The_real_renderer_over_the_golden_corpus_rejects_the_two_diagrams_github_renders (skips with a reason when node_modules is absent). A 50% rejection rate on the golden corpus is what makes (D)'s 59-diagram count worth having before M2 publishes anything.
WHY CONVERSION CANNOT SEE IT (do not "fix" the converter): the attachment name is a pure function of the diagram source (§8), so WikiTree.DiagramResolver never returns null and a page with an unrenderable diagram converts perfectly clean, then fails at publish. Two passes is the design, not a gap.
THE TWO AXES, same shape as the §4.4 runner: failures group BY THE RENDERER'S REASON (its message with quoted tokens normalized out) and break down BY DIALECT = the diagram's FIRST NON-EMPTY LINE, trimmed. Probed on 1.1.3: EVERY rejection is one message shape, 'Invalid mermaid header: "<first line>". Expected "graph TD", …', so normalizing the quotes collapses every refusal into ONE bucket while the dialects stay apart. Cost of a verbatim first line: a header carrying a title ('pie title Pets') buckets per title — visible as several buckets of one, which still reads "pie is unsupported". Renders 'graph/flowchart', 'sequenceDiagram', 'classDiagram', 'stateDiagram-v2', 'erDiagram', 'xychart-beta'; refuses 'pie', 'gitGraph', and ANY header with a trailing semicolon ('graph LR;' too, not just 'graph TD;').
QuotedTokens IS NOW SHARED BY BOTH HALVES: the converter quotes its dialect with ' and beautiful-mermaid quotes the refused header with ", so one normalizer serves both. ConversionAcceptance.Describe now calls it; behavior is identical and its existing tests pin that.
MermaidRenderFault {Diagram, Setup} IS A CONSTRUCTOR PARAMETER on MermaidRenderException, deliberately not a defaulted property and deliberately not sniffed from the message at the catch site. A missing Node / missing script / uninstalled beautiful-mermaid / non-SVG stdout fails EVERY diagram identically, so tallying those as findings would report a broken machine as a broken corpus; only Fault==Diagram becomes a report row, and Setup propagates and stops the pass (CLI prints it and exits 1). A TIMEOUT is classified Diagram on purpose (one diagram's pathology; a genuinely stuck Node shows up as every diagram timing out). ANY NEW THROW SITE MUST CLASSIFY ITSELF — that is why the parameter is required.
SOURCES COME FROM WRAPPING THE RESOLVER, NOT A SECOND PARSE: ConversionAcceptance.ConvertOne wraps page.Resolvers.Diagram in a collector, so PageConversionResult.Diagrams (4th positional param, new) carries every fence body in render order and AcceptanceReport.Diagrams flattens them to DiagramOccurrence(Path, Source). Like the diagnostics, a page that later fails loud KEEPS the diagrams it collected (test: Keeps_the_diagrams_a_page_collected_before_it_failed).
ONE RENDER PER DISTINCT SOURCE (test counts invocations via a logging stub): 59 fences is not 59 Node round-trips. Sequential on purpose — deterministic, readable output beats a few seconds.
THE BAR NOW SPANS BOTH PASSES: MeetsAcceptanceBar gained '&& (Renders?.AllRendered ?? true)'. A conversion-only run CAN still be MET while holding a diagram that will fail at publish, so the verdict line NAMES the unchecked count ("MET — 1 pages, 0 errors, 0 warnings, 2 diagram(s) unchecked") and the report prints "MERMAID DIAGRAMS — N found, NOT CHECKED". Do not quietly drop that caveat.
THE REPORT ALSO PRINTS A DIALECT CENSUS over every diagram, rendered or not: the question is which dialects a corpus uses, not only which broke.
CLI PROBED END TO END on scratch wikis under .mtk/ (deleted): no flag -> counted + NOT CHECKED + MET-with-caveat; --render-mermaid over 'graph TD;' -> NOT MET, exit 1, dialect named, page named; all-clean corpus -> MET, exit 0; missing script -> one setup-fault line, exit 1, no bogus findings.
DIALECT SHIM STILL NOT ADDED, and still should not be without (D)'s numbers: the recorded precedent (.claude/references/decisions.md, fence dialects) is COUNT FAILURES BY DIALECT FIRST, THEN DECIDE. A failed page is the safe direction; a wrong diagram is not. If (D) shows the AurServices corpus is mostly 'graph TD;', the cheap fix is a documented source normalization (strip a trailing semicolon from the HEADER LINE ONLY) with its own golden — a decision for real numbers.

=== WHY M2 STARTS WHILE THE SANDBOX GATES ARE STILL OPEN (iter26 sequencing decision) ===
M2's implementation does not depend on the sandbox; its acceptance does. §4.2: "Confluence HTTP is tested with WireMock.Net. Live-sandbox tests are opt-in only (env-gated), never part of the default dotnet test run." So slice 1 (below) is fully verifiable offline. The client is ALSO orthogonal to whatever (D) reveals — it does not care what bytes the storage format contains — so building it does not risk being invalidated by the acceptance run.
WHAT STILL CANNOT BE DONE WITHOUT THE SANDBOX, and must NOT be faked: M2's acceptance ("publish DocuMe's own docs to a sandbox space, then the Aur bulk publish human-reviewed page-by-page"), and the 13 storage-format round-trip questions listed below. On the §8 contentHash: iter27 relaxed iter26's blanket "do not build it against guesses" — see the reasoning at the end of the SLICE 2 block above (S6 hashes source-derived content pre-upload, so sandbox item (5) cannot destabilize it), but the banner half still needs §8 read closely.
M2 SLICES 1-3 ARE ALL SHIPPED; the client is feature-complete for M2. See the top of this field for slice 4.
AFTER THE CLIENT, THE PIPELINE (§6.2) — and one iter26 caution can be relaxed: §8 contentHash IS buildable before the sandbox pass. §13 S6's own resolution is "hash the source-derived content (pre-upload), never re-read body for hashing — design already assumes this", so whether Confluence re-encodes non-ASCII on save (sandbox item (5)) cannot destabilize a hash we compute from our own rendered storage format and compare against state.json. Item (5) still matters for anything that COMPARES A READ-BACK BODY, and the banner injection still needs §8's exclusion rule to be exact (§9.2), so read §8 closely rather than treating this as a green light for the whole banner slice.

=== WHAT IS DELIBERATELY NOT THE NEXT INCREMENT (judged at iter26, do not re-litigate without new evidence) ===
ac:width ON MERMAID IMAGES (spec finding (6)) AND '{width=300}' ON IMAGES (finding (5)) ARE BOTH DEFERRED, not forgotten. MermaidRenderer already exposes SvgWidth/SvgHeight so the data is in hand, but BOTH would churn a hand-reviewed golden (§4.3) on an UNVERIFIED external behavior: sandbox item (8) asks whether Confluence INJECTS ac:width on an ac:image by itself and whether an SVG attachment is treated differently. Emitting a width that Confluence then rewrites is §8 hash drift on every diagram page. Same precedent as iter23's brush decision: verify, then emit. Do these in the sandbox pass, not before.
THE ELEVEN-NOW-THIRTEEN DECISION ENTRIES still cannot be written (.claude/ write protection) — see blockers.

=== (D) §4.4 ACCEPTANCE RUN — STILL GATED, gate id gate-m1-aurservices-files ===
Opened iter21 in GATES.md ("Open gates"), mirrored in state.gates, unchanged since. The AurServices wiki markdown is not in this repo; the run cannot happen until Mirko copies it somewhere readable and sets `acceptance.aurServicesWikiPath` in this file (that key still does not exist). ONCE SET, (D) IS TWO COMMANDS:
  dotnet src/DocuMe.Cli/bin/Release/net10.0/DocuMe.Cli.dll convert <aurServicesWikiPath>
  dotnet src/DocuMe.Cli/bin/Release/net10.0/DocuMe.Cli.dll convert <aurServicesWikiPath> --render-mermaid --renderer templates/tools/render-mermaid.mjs
Run the second one: as of iter26 it answers the diagram question in the same pass. Read the grouped output, record the counts here, and only then decide (a) which diagnostic codes deserve --accept, (b) whether §4.4's second clause needs a per-code allowlist or a severity split, and (c) how many of the 59 diagrams beautiful-mermaid rejects and whether a header-semicolon normalization is worth it. Read-only, nothing written back to those files.
EXPECT TREE ERRORS FIRST on a real 79-page wiki: WikiTree.Load validates titles and attachment-name collisions before any conversion, and the command reports those instead of a report. They are real findings (a duplicate title cannot publish), not a runner bug.
THE RENDER PASS WILL TAKE ~30-60s on 59 diagrams (one Node process per distinct source, sequential). That is expected, not a hang.

=== (H) THE §4.4 RUNNER, SHIPPED ITER24 — READ BEFORE EXTENDING THE REPORT ===
'docume convert <wiki-root> [--accept <code>]' plus src/DocuMe.Core/Acceptance/. Read-only: renders storage format and throws it away, no credentials, no network.
THREE ENTRY POINTS AND WHY: ConversionAcceptance.Run(IEnumerable<AcceptancePage>) is the primitive and takes PAGES, not a directory, so the runner works over a corpus WikiTree.Load would reject — load-bearing, because the flat golden fixture directory has no per-file titles and IS the runner's test corpus. RunDirectory(dir, sharedResolvers) is the fixture/triage mode (resolvers shared, so relative links resolve against the corpus root — wrong for a nested wiki, fine for a flat one). RunTree(WikiTree) is the real §4.4 path and binds per-page resolvers.
FAILURES GROUP BY MESSAGE, NOT BY CODE, and that was the trick that avoided touching the converter: the renderer's ~15 fail-loud sites throw NotSupportedException with PROSE. ConversionFailure.Kind is that message with quoted tokens normalized to '…' (now via QuotedTokens), Token is the first quoted token, so one derivation gives BOTH axes. IF THE GROUPING EVER PROVES TOO COARSE ON A REAL CORPUS, the follow-up is structured failure codes on the throw sites, and Kind is the only thing that moves.
ONLY NotSupportedException BECOMES A REPORT ROW. Anything else keeps crashing the run, deliberately: a converter bug quietly tallied as an acceptance finding would read as "that page needs different markdown". Do not widen that catch. (Same rule, same reasoning, as MermaidRenderFault above.)
SEVERITY IS A POLICY, NOT A DIAGNOSTIC PROPERTY: AcceptancePolicy maps codes -> DiagnosticSeverity {Warning, Note}. A note is still counted and printed with its full dialect breakdown; it just does not break MeetsAcceptanceBar. CLI: --accept <code>, repeatable.
BAR SEMANTICS: PageCount > 0 && FailedPageCount == 0 && WarningCount == 0 && every rendered diagram OK. An EMPTY corpus deliberately does NOT clear it (tested). Exit 0 = met, 1 = not met, so the command doubles as a consumer CI pre-flight. A tree that cannot load prints its WikiTreeException errors as findings and exits 1 rather than a stack trace.
THE RUNNER'S CONTRACT TEST is Reports_exactly_the_known_degradations_of_the_golden_corpus: the exact seven 'page | code | construct' lines in report order. Treat that list like a golden (§4.3) — a new golden case that changes it is telling you something.
GoldenCorpus (tests/DocuMe.Core.Tests/Markdown/GoldenCorpus.cs) holds the corpus directory + the three resolver maps, shared by both suites. BundledRenderScript.cs (new at iter26) does the same for "where is the real render script and is beautiful-mermaid installed", shared by MermaidRendererTests and MermaidAcceptanceTests. Use both rather than re-deriving.

=== (G) FOUR MORE DIAGNOSTIC SITES, SHIPPED ITER25 ===
Commit 115a162. FOUR CODES: table-alignment-dropped, ordered-list-start-dropped, alert-type-collapsed, task-list-numbering-dropped. Every site asserts the emitted XML is byte-identical to before, so no golden moved.
TWO CAME OUT NARROWER THAN iter22 ASSUMED — DO NOT WIDEN EITHER WITHOUT NEW EVIDENCE:
  - TABLE ALIGNMENT REPORTS CENTER AND RIGHT ONLY. Markdig leaves ColumnDefinition.Alignment NULL for a plain '|---|' and fills it only when the author wrote a colon, so an explicit '|:--|' IS distinguishable and publishes left-aligned, exactly as GitHub renders it — it loses nothing and stays silent. Reported once PER COLUMN.
  - ONLY [!IMPORTANT] REPORTS THE ALERT COLLAPSE, not [!NOTE]. Of five markers, IMPORTANT is the only one whose panel ('info') is already owned by another marker, so an IMPORTANT arrives looking like a NOTE while a NOTE keeps its meaning. CollapsedMarkers is a LISTED dictionary, deliberately not derived from AlertPanelMacros.
  - CONSTRUCT SPELLINGS: the list codes use the AUTHORED MARKER ('3.', '1)'), the alert code uses the marker verbatim INCLUDING CASE, the table code uses 'center'/'right'.
  - AN ORDERED TASK LIST REPORTS ONE LOSS, NOT TWO: task-list-numbering-dropped subsumes ordered-list-start-dropped.
ALL FOUR DEFAULT TO Warning. Demoting any costs ONE '--accept <code>' entry; only (D)'s numbers should decide.
NOT candidates, decided at iter22/25 with reasoning in the XML doc at each site — do not re-litigate: reference definitions, a nested GitHub alert, a non-root '[TOC]', an unrecognized character reference, a dropped inline comment's leftover double space, mark's '-' fence language, an explicitly-left table column, a '|---|' header dash count, '1)' vs '1.', and [!NOTE]/[!TIP]/[!WARNING]/[!CAUTION].

=== DECISION LOG ENTRIES THAT COULD NOT BE WRITTEN — NOW TWENTY-TWO PENDING ===
The .claude/ write blocker is still live (not re-tested at iter26). NINETEEN entries are owed to .claude/references/decisions.md:
  - iter17 "2026-07-24 — Unknown fence languages: cosmetic loss degrades, semantic loss fails loud"
  - iter18 "2026-07-24 — HTML comments: drop comment-only blocks, fail loud on a shared line"
  - iter19 "2026-07-24 — HTML entities: publish the resolved character, not the entity name"
  - iter20 "2026-07-25 — Mermaid: the attachment name is a pure function of the diagram source; beautiful-mermaid's dialect subset fails the page rather than being shimmed"
  - iter21 "2026-07-25 — Attachment names flatten the whole path: a pure function of the path beats a readable name, because collision-triggered renaming would revoke approvals on unedited pages"
  - iter22 "2026-07-25 — Conversion diagnostics report only losses against GitHub's own rendering"
  - iter23 "2026-07-25 — Code-fence brushes require two-source confirmation (Atlassian's documented list AND a Prism component id), because a guessed brush is a silent no-highlight that also suppresses its own diagnostic"
  - iter24 "2026-07-25 — Acceptance severity belongs to the run's policy, not to the diagnostic"
  - iter24 "2026-07-25 — Fail-loud failures group by their message with quoted tokens normalized out"
  - iter25 "2026-07-25 — A degradation is reported per lost pixel, not per degraded construct"
  - iter25 "2026-07-25 — Of two alert markers sharing one Confluence panel, only the marker that does NOT own the panel reports"
  - iter26 "2026-07-25 — Rendering is a second acceptance pass, not a converter feature: because the diagram attachment name is a pure function of the source, conversion structurally cannot detect an unrenderable diagram, so §4.4 needs an opt-in render pass and the bar spans both"
  - iter26 "2026-07-25 — A render fault is classified at the throw site, never sniffed from the message: a broken setup rejects every diagram identically, so a report tallying it as findings would read as a broken corpus instead of a broken machine"
  - iter27 "2026-07-25 — Spike S1 closed on evidence: Dapplo.Confluence is maintained and targets net10.0, but hard-wires its API root to /rest/api (v1) and carries Newtonsoft via Dapplo.HttpExtensions, so DocuMe writes the thin v2 client §4 asked for"
  - iter27 "2026-07-25 — A server-side filter's result is re-checked client-side when its documentation does not promise exactness: the v2 title filter may match loosely, and the safe direction is a lookup that misses (ending in a create Confluence rejects loudly) rather than one that updates the wrong page"
  - iter27 "2026-07-25 — Retry policy lives in the HttpClient handler pipeline and its transient predicate is hand-written, not the library's: the 401/403-never-retry rule then cannot be widened by a package upgrade, and the tests exercise the real pipeline through WireMock"
  - iter27 "2026-07-25 — Credentials are not a record and expose no token property: a generated ToString would print the secret into the first log line that formats the object"
  - iter28 "2026-07-25 — A version conflict is reported, never resolved: re-reading the version and pushing again would always succeed, and that is the cost — it erases the only signal that a human or a second run was editing, and lets two publish runs interleave two repo states silently"
  - iter28 "2026-07-25 — Retrying a non-idempotent create is bounded by the server's own uniqueness constraint: Confluence enforces one title per space, so a replayed POST whose first attempt landed returns a loud duplicate title instead of a silent second page"
  - iter29 "2026-07-25 — The attachment write uses v1's create-or-update verb, not its create verb: because the pipeline uploads only hash-changed attachments, the filename almost always exists already, so the create-only verb would reject exactly the uploads that matter"
  - iter29 "2026-07-25 — A replayable request body is a design constraint, not an implementation detail: the multipart upload is built from buffered parts because a streamed part would be drained by the first attempt and upload an empty file over a working attachment on the retry"
  - iter29 "2026-07-25 — Verify-then-emit governs output, not inert headers: a guessed storage-format attribute becomes a wrong artifact and hash drift, while an undocumented-but-inert request header costs nothing, so the XSRF header is sent on the v1 label write that does not document needing it"
An interactive session (or the loop, once Write(.claude/**) is allowlisted) should promote all twenty-two.

=== SANDBOX GATES — FIVE UNCHECKED, ALL MIRKO'S CLICKS ===
Read GATES.md "Setup Mirko must do before M2" and .claude/references/decisions.md "2026-07-24 — Sandbox is a dedicated disposable space" before acting. THE DECISION: a dedicated disposable space "DocuMe Sandbox", key DOCUMESBX, on MOBERG's own Confluence tenant — explicitly NOT the existing ITAPPS space, because 'publish --prune' deletes orphan pages (§6.2) and §9.1 makes republish overwrite hand edits, so pointing the loop at any space holding human content is a real data-loss path. FIVE ITEMS: create the space; set confluence.sandboxSpaceKey = "DOCUMESBX"; set a NEW confluence.sandboxBaseUrl field; create a Moberg-tenant API token; export DOCUME_CONFLUENCE_EMAIL + DOCUME_CONFLUENCE_TOKEN and RESTART the loop.
THREE CONSEQUENCES THE LOOP MUST ACT ON: (a) CROSS-TENANT — the sandbox is on a different Atlassian tenant than production AUR (kvika.atlassian.net), so NEITHER token NOR base URL is shared, and the M7 cutover swaps credentials AND base URL, not just the space key. (b) SCHEMA GAP — state.json -> confluence has no sandboxBaseUrl field. GATES.md's "thread it through the config loader" needs interpretation: DocumeConfig/ConfigLoader read the CONSUMER repo's docume.json, whereas state.json -> confluence is LOOP bookkeeping no production code reads. The real work is deciding how an iteration turns its sandbox key + base URL into the docume.json or CLI flags a verification publish uses — that is publish wiring, and it cannot be verified until the publish path exists. Do it in the slice that first publishes, not speculatively. (c) CREDENTIALS DO NOT REACH A RUNNING LOOP — verified 2026-07-24 that the loop process has neither env var, and each iteration reads state at start and rewrites it at the end, so a mid-iteration edit to state.json is LOST. Mirko must edit BETWEEN iterations and relaunch from a shell that exports both vars.
M2 SLICES 1 AND 2 NEED NONE OF THIS (slice 1 shipped without it at iter27). Only end BLOCKED when an iteration needs an actual HTTP call to a real space and the key is still null.

=== SANDBOX VERIFICATION ITEMS: SEVENTEEN (none blocking; all for the first sandbox pass) ===
(17) NEW AT ITER29 — DOES THE LABEL POST RETURN ONLY THE ADDED LABELS OR EVERY LABEL ON THE PAGE? v1 says only "Returned if the labels are added to the content". AddLabelsAsync returns whatever came back rather than assuming which, so nothing is wrong either way — what the answer settles is whether the approval sync (§6.3) can use the POST response as the page's full label set or must follow with a read. Add 'approved' to a page that already has 'stale' and count the results.
(16) NEW AT ITER29 — WHICH FILENAME DOES CONFLUENCE STORE? .NET emits the file-name parameter TWICE on the multipart part: plain 'filename=<name>' plus the RFC 5987 'filename*=utf-8''<name>'. Servers may honour either. Upload an attachment whose flattened name is long and underscore-heavy (docs_architecture_data_flow.svg) AND one with a non-ASCII character, then read back ri:filename and check it matches byte-for-byte — a mismatch breaks the <ri:attachment ri:filename="…"> reference in the page body, which is a broken image on a page that published "successfully". Folds in the iter21 question about long flattened names.
(15) NEW AT ITER28 — DOES THE v2 UPDATE ENDPOINT ACTUALLY STORE version.message? UpdatePageAsync sends it when a caller supplies one, but an Atlassian community thread reports v2 dropping it ("Update page -> version comment not processed"). Update a sandbox page with a message, then GET /api/v2/pages/{id}/versions and see whether it came back. If it is dropped, the publish audit trail cannot live there and ConfluencePageRevision.VersionMessage should go rather than sit there looking useful. Cheap, and the XML doc already warns nobody to build on it unverified.
(14) NEW AT ITER28 — WHICH STATUS DOES A STALE VERSION ACTUALLY PRODUCE? The v2 document lists NO 409 for ANY operation, yet Atlassian's developer community documents 409 "Version must be incremented when updating a page". Read a sandbox page, edit it in the browser, then PUT with the now-stale version+1 and record the STATUS and the BODY. Both paths are already handled (409 -> ConfluenceConflictException; anything else -> ConfluenceApiException naming the page and version), so nothing breaks either way — what the answer settles is whether the 409 branch is live code or defensive, and whether the publish pipeline can pattern-match on it.
(13) NEW AT ITER27, and the client already guards against the bad answer: IS THE v2 /pages 'title' FILTER AN EXACT MATCH? Publish two pages, "Costs" and "Costs and billing", then GET /api/v2/pages?space-id=<id>&title=Costs&status=current and see whether one or both come back. FindPageByTitleAsync re-checks the returned title, so a loose filter costs nothing today; what the answer changes is whether that guard can be dropped (exact) or whether the limit=1 needs raising so the right page is not paged out behind a near-match (loose). Cheap, and it removes a permanent maybe from the publish path.
(12) NEW AT ITER26, and it decides two deferred features at once: does Confluence INJECT or REWRITE ac:width on an <ac:image> that wraps an <ri:attachment>, and is an SVG attachment treated differently from a PNG? Publish one page with a bare <ac:image> over an SVG and one over a PNG, read both back byte-for-byte. If Confluence injects a width, emitting our own (spec findings (5) and (6)) is §8 hash drift on every diagram page and both stay deferred; if it leaves the markup alone, both become straightforward slices with a golden update each. MermaidRenderer already exposes SvgWidth/SvgHeight, so no new investigation is needed once this is answered.
(11) iter25 — does Confluence storage format honor '<ol start="3">'? DocuMe emits a bare <ol>, so an ordered list starting past 1 publishes numbered from 1 and reports ordered-list-start-dropped. If Cloud keeps the attribute, that diagnostic site goes away and the converter gains one attribute (ListRenderer + one golden). If it strips it, the diagnostic is confirmed correct. Publish one page with '3. third' and read it back.
(10) iter23 — BRUSH SPELLINGS. Confirm whether Cloud honors every Prism id or validates against a narrower internal table, specifically the INFERRED pairings: objectivec, latex (from 'tex'), docker (from 'dockerfile'), cfscript (from 'coldfusion'), rest (from 'rst'), sml (from 'standardml'), visual-basic vs vbnet, scss vs sass, splunk-spl, mathematica, hcl (from 'terraform'). WORST CASE IS BENIGN — an unhonored brush publishes the unhighlighted block we already publish today. Also check the pre-existing ['txt'/'text'/'plaintext'] -> "text" mapping: Atlassian spells that entry "PlainText" and 'text' is NOT a Prism component, so it may already be a silent no-highlight.
(9) iter20 — the rendered SVG carries <style>@import url('https://fonts.googleapis.com/css2?family=Inter…')</style>, AN EXTERNAL NETWORK REFERENCE INSIDE PUBLISHED CONTENT. Confirm whether Confluence strips, blocks or honors it, and whether the diagram stays legible when the font does not load. beautiful-mermaid's 'font' RenderOptions can avoid the remote import; render-mermaid.mjs does not set it today. Same pass as (8) and (12).
(8) MERMAID: inline SVG via <ri:attachment> vs a forced download link (drives MermaidAttachmentName.Extension — .svg today, one const to change), whether <p><ac:image>…</ac:image></p> survives a save, and whether an SVG attachment gets ac:width injected (now folded into (12)).
(5) HIGHEST VALUE: the converter emits NON-ASCII in page bodies (©, —, →, …, U+00A0, astral emoji). Confirm Confluence round-trips them byte-identically rather than re-encoding as numeric character references — a server-side re-encode is §8 hash drift on any page with a symbol, and U+00A0 is the likeliest to be normalized. THIS ONE GATES THE §8 contentHash SLICE: do not build hashing against a guess.
(1)-(4),(6),(7) stand as written in git history: empty <ac:rich-text-body> (and empty <blockquote>); omitted code-macro collapse/linenumbers params; ac:task-id acceptance/reassignment; empty <ac:task-body>; strikethrough <span style="text-decoration: line-through;"> byte-stability; paramless toc macro self-closing rewrite + ac:schema-version/ac:macro-id injection; the <a>-wrapping-<ac:image> composition + attribute ORDER + injected ac:width + a ri:filename with a space and an '&'. The attribute-tolerant normalizer that (6)(7)(8)(12) all demand should be built ONCE for all macros and ac:image together.
FROM iter21: attachment filenames now contain UNDERSCORES AND MAY BE LONG (images_sub_deep.png). Confirm Confluence accepts a long flattened name on ri:filename unchanged.

=== SPEC FINDINGS: NINE OPEN, ALL NON-BLOCKING, NONE EDITED BY THE LOOP ===
(9) iter20, WAS THE HIGHEST-RISK ONE, NOW HALF-CLOSED — §4 line 128 says the mermaid mechanism is "Proven on AurServices (59 diagrams)", but beautiful-mermaid 1.1.3 rejects a header semicolon and 'pie'. iter26 built the pass that measures it and proved 2-of-4 on the golden corpus; the AurServices number is (D)'s to produce. The §4 line itself is what needs amending once (D) runs, and the loop does not edit PLAN.md.
(10) iter24 — §6 lists init/publish/sync/drift/dashboard/status and has NO subsection for 'docume convert', which now carries three options (--accept, --render-mermaid, --renderer). It is a read-only acceptance/pre-flight command (§4.4), not a lifecycle command, so it plausibly belongs as §6.7 or a note under §7. A one-subsection addition.
(3) iter14 — §7 line 285 lists strikethrough under "Native XHTML", which reads as <s>/<del>; the ACTUAL Confluence spelling is <span style="text-decoration: line-through;"> and DocuMe emits the span.
(4) iter15 — §7 line 292 names kovetskiy/mark as the behavioral reference for the construct table, but mark has NO "[TOC]" shorthand at all, so §7 line 284's row has no reference-tool behavior to copy and iter15 defined the semantics itself.
(5) iter16 — §7 line 287's images row promises "optional width via {width=300} attribute -> ac:width". That syntax needs a Markdig extension DocuMe does not enable, so it currently fails loud. DEFERRED ON PURPOSE at iter26 — see sandbox item (12).
(6) iter17, settled in principle by iter20 — §7 line 281's mermaid row shows <ac:image ac:width="…">. Width IS obtainable (MermaidDiagram exposes SvgWidth/SvgHeight); what remains is a seam change plus a golden update. DEFERRED ON PURPOSE at iter26 — see sandbox item (12).
(7) iter18 — §7 line 290's HTML-comments row reads as though '<!-- c --> tail' just drops the comment. §7 is SILENT on the shared-line and unterminated cases, so slice 12 decided them (fail loud). A one-clause addition would make the row match.
(8) iter19 — §7's construct table HAS NO ROW FOR CHARACTER REFERENCES AT ALL. Method lesson: §7's table being complete does NOT mean the converter accepts every page; the remaining unknowns are exactly the things the table does not think of as constructs.
FROM iter22, a §4.4 WORDING problem rather than a converter one: §4.4 says "zero errors and zero unknown-construct warnings", but the converter's accepted losses are warnings that CANNOT reach zero without new features nobody has asked for. iter24's severity tier is the MECHANISM for resolving it; the remaining question is purely which codes belong in the accepted set — a decision (D)'s numbers should make.
The two corrections a delegated interactive session made on 2026-07-24 (§7 code-fence row, §7 task-list row) are DONE and recorded in decisions.md.

FINDING 9 (opened iter30, ALREADY IMPLEMENTED AS A DEVIATION, needs a one-line PLAN edit by a human): §6.2 step 5 says "Compute contentHash. If unchanged vs state and not --force -> skip (log)." Taken literally that also skips the attachment upload, but contentHash covers the page BODY only. A hand-placed image keeps its filename when its bytes change (unlike a mermaid diagram, whose filename is derived from its source), so a literal skip republishes nothing and the page keeps showing the old picture indefinitely. PublishPlanner therefore emits a fourth action, UpdateAttachments: upload the changed bytes, do not write the body, do not consume a page version, do not touch approval (§8 keys invalidation off contentHash, and this path did not move it). SUGGESTED PLAN EDIT, for whoever next edits PLAN.md: step 5's skip clause should read "skip the body; still upload attachments whose hash changed". The loop does not edit PLAN.md.

=== BUILD/TEST COMMANDS ===
'dotnet build DocuMe.slnx -c Release' and 'dotnet test --solution DocuMe.slnx -c Release'. xUnit v3 on the Microsoft Testing Platform, so 'dotnet test DocuMe.slnx' (solution POSITIONAL) FAILS with "Specifying a solution for 'dotnet test' should be via '--solution'"; a bare 'dotnet test' from the repo root also works. Output is MTP's summary shape ('Test run summary: Passed! total: 388'). TEST COUNT IS NOW 420 (411 at iter27): +9 write-path cases in ConfluenceClientTests.
RUNNING THE CLI WITHOUT dotnet run: 'dotnet src/DocuMe.Cli/bin/Release/net10.0/DocuMe.Cli.dll <args>' works from the repo root and avoids a rebuild. Verified again at iter26 for 'convert', 'convert --help', '--render-mermaid', '--renderer' and the setup-fault path.
FOR FULL COVERAGE RUN 'npm ci' AT THE REPO ROOT FIRST. Without node_modules the three real-script MermaidRenderer tests AND iter26's real-renderer acceptance test SKIP with a reason ('skipped: 4'). iter26's runs reported 'skipped: 0', so node_modules is present on this machine. CI MUST RUN 'npm ci' BEFORE 'dotnet test'.
MTP FLAGS confirmed working: --filter-class '*MermaidAcceptanceTests*', --filter-method '*Something*', and --output Detailed to print per-case names. USE --output Detailed ON EVERY NEW TEST FILE to prove the cases actually RAN rather than being silently skipped. --output Detailed goes DIRECTLY on the dotnet test command line, NOT after a '--' separator. --filter-method DOES NOT ACCEPT AN ALTERNATION.
BASH GOTCHAS (all still true at iter26): the Bash tool's working directory PERSISTS ACROSS CALLS — ALWAYS pass absolute paths or prefix with 'cd /Users/mirkobudimir/Dev/docu-me &&'. A COMPOUND command joined with ';' is DENIED; 'cmd || echo "exit=$?"' is DENIED (read the tool's own "Exit code N" line instead); a two-part 'sed -i' is DENIED; NEW AT ITER26 — even a single 'sed -i ... && dotnet build' compound is DENIED (use the Edit tool with replace_all instead of sed). 'sed -n' RANGE PRINTING IS DENIED — use 'grep -n -A <n> <pattern> <file>'. A shell FUNCTION DEFINITION and a 'for' loop are each DENIED. A 'cd X && npm ...' compound is DENIED; use 'npm install --prefix <abs-dir>'. 'rm -rf <dir>' is blocked by the MTK security-gate hook but PLAIN 'rm -r <dir>' IS ALLOWED (iter26 used one 'rm -r' for three probe paths at once). 'git status --short --cached' is not valid git (use 'git diff --cached --stat'). 'cat -A' is not macOS-valid (use 'od -c'; 'xxd' is denied). 'python3' is denied but 'node' is allowed — iters21-26 rewrote this file with a node ESM script at .mtk/update-state.mjs, run as 'node <abs path> <timestamp>', deleted afterwards; that is the pattern for any big state.json edit. A 'git commit -F -' HEREDOC IS REFUSED if the message contains a NBSP; 'git commit -m <subject> -m <body>' with multi-line quoted bodies WORKS (escape inner double quotes; avoid backticks). READING state.json: the Read tool FAILS on it (it exceeds the 25k-token cap) — use 'node -e' to print keys or one field.
NEW ANALYZER GOTCHAS FROM ITER27 (all errors): S1075 "remove this hardcoded path-delimiter" fires on a plain string literal that STARTS WITH '/' or holds one when assigned to a path-ish name — an INTERPOLATED string escapes it, which is why the client builds every request path with $"api/v2/…" and the tests build theirs with $"/wiki/api/v2/{endpoint}"; a bare "/" literal needs a named const char instead. CA1859 demands a private factory return the CONCRETE type (ResilienceHandler, not HttpMessageHandler). MA0006 rejects '==' between strings even inside a lambda (use string.Equals with StringComparison.Ordinal). CA1829 + CA1826 both fire on .Count() over an indexable collection. Shouldly's ShouldNotBeNull does NOT narrow nullability for the compiler — the house shape is 'x.ShouldNotBeNull(); var y = x!;' (already used in MermaidAcceptanceTests).
NEW ANALYZER GOTCHAS FROM ITER28 — A CONTRADICTORY PAIR, worth knowing cold because it costs two build cycles to discover: validating a FIELD of a parameter (draft.SpaceId) with ArgumentException.ThrowIfNullOrWhiteSpace fails MA0015 "the expression does not match a parameter" (CallerArgumentExpression yields 'draft.SpaceId', which is not a parameter name), and passing the parameter name explicitly to satisfy it then fails S3236 "remove this argument, it hides the caller information". THERE IS NO SPELLING OF THE .NET THROW HELPERS THAT SATISFIES BOTH on a member expression. The resolution in ConfluenceClient is a private Validate(record, parameterName) overload per input type over RequireField/RequireBody helpers, which also lets the message name the FIELD while paramName stays the record — reuse those for the attachment/label inputs instead of rediscovering this.
MTP FILTER GOTCHA FROM ITER27: --filter-class '*Confluence*' ran 239 of the 411 tests rather than the 23 Confluence ones — a loose wildcard matches far more than it looks like it should. Use the class name ('*ConfluenceClientTests*'), and read the 'total:' line to confirm the filter did what you meant.
LSP NOISE, CONFIRMED AGAIN AT ITER26 AND ITER27: after adding a NEW FILE that defines types other new files reference, csharp-lsp reports a wall of bogus CS0246/CS1061 "type could not be found" diagnostics for those types, and keeps reporting them for the rest of the session. They are STALE INDEX: 'dotnet build' compiled the same files with 0 warnings/0 errors. TRUST THE BUILD.
ANALYZER GOTCHAS (all are ERRORS, not warnings). NEW AT ITER26: RCS1118 demands a local with a literal initializer be 'const'; MA0042 + S6966 together forbid File.ReadAllLines/ReadAllText in an async method (use the Async overload and pass TestContext.Current.CancellationToken); SA1515 also fires on a single-line comment placed inside an ARGUMENT LIST (a blank line does not help — move the reasoning into the member's XML doc, same fix as iter25's collection-expression case). STILL TRUE: SA1118 'parameter spans multiple lines' fires on a multi-line argument UNLESS it is the ONLY argument — assign a multi-line raw string body to a LOCAL first, then pass it; a probe test method named the same as a private helper fails xUnit1024; a probe [Fact] with no assertion fails Sonar S2699; [GeneratedRegex] needs a match timeout (MA0009) and RegexOptions.ExplicitCapture (MA0023, forcing NAMED groups); Sonar S3267 fails a foreach whose body only reads ONE property of the loop variable (prefer LINQ); Shouldly's Should.Throw takes an Action so a method group with an OPTIONAL parameter does not convert (write a lambda); Dictionaries AND HashSets with string keys need an explicit StringComparer (MA0002); a collection expression cannot be the branch of a 'var' ternary; Sonar S4144 fires across [Theory] methods with IDENTICAL BODIES; SA1512 forbids a single-line comment followed by a blank line.
SHAPES THAT PASS CLEANLY (iter23-26 evidence): a ~70-row [InlineData] theory with grouped comments; iter24's 5-file 921-line LINQ-heavy slice; and iter26's 5 new/7 modified files with async plumbing, collection-expression spreads ([.. query]), computed record properties and a required enum ctor param — the last hit only the four analyzer rules above, all mechanical. Prefer LINQ over foreach, spell every string comparison with StringComparison.Ordinal / StringComparer.Ordinal, and assign '_ =' to ignored return values and StringBuilder chains.
PROBE METHOD (12 slices running, unbeaten — do this before hand-authoring a golden or committing to an external library's behavior): dump the REAL behavior for every spelling you plan to support, from a throwaway under the gitignored .mtk/. iter20 aimed it at a NODE PACKAGE, iter21 at a NUGET API, iter22 at its own new feature, iter23 AT THE WEB, iter24 at its own new CLI, iter26 AT beautiful-mermaid DIRECTLY (11 dialect spellings through one ESM script, which is what revealed that every rejection shares one message shape and that a leading blank line is tolerated) AND THEN at the CLI over two scratch wikis (four paths in four commands, then 'rm -r'). Delete the probe before committing.
WEB ACCESS WORKS IN THE HEADLESS LOOP (iter23): WebFetch AND WebSearch both. Highest-value URLs for brush questions: https://raw.githubusercontent.com/PrismJS/prism/master/components.json, https://confluence.atlassian.com/doc/code-block-macro-139390.html, https://support.atlassian.com/confluence-cloud/docs/insert-the-code-block-macro/. unpkg.com/@atlaskit/code paths 404'd and data.jsdelivr.com returned 400 — do not retry those.

=== CONVERTER + LINK MAP + DIAGNOSTICS + ACCEPTANCE CAPABILITY ===
RENDERS: headings (H1 dropped), paragraphs, emphasis (bold/italic/strikethrough), inline code, XML-escaped literals, hard/soft breaks, bullet/ordered/nested lists (tight vs loose via renderer.ImplicitParagraph), task lists (native <ac:task-list>, page-unique monotonic ac:task-id, mixed lists degrade with literal markers), blockquotes, GitHub alert panels (root-level only), fenced code blocks (code macro + LanguageMap aliases + fence attributes collapse/nocollapse/linenumbers/firstline/'title <rest of line>'), thematic breaks, links (external <a href> incl. title=, relative .md -> <ac:link><ri:page> via PageLinkResolver, same-page #anchor -> text only, autolinks, reference-definition no-op), GFM tables, root-level '[TOC]' -> paramless toc macro, images -> <ac:image> with <ri:attachment> or <ri:url>, mermaid fences (backtick and tilde) -> <p><ac:image><ri:attachment/></ac:image></p> via MermaidDiagramResolver, HTML COMMENTS in both forms, and HTML ENTITIES (resolved character, everywhere).
LANGUAGE MAP: ~120 fence spellings across ~70 brushes. SEE THE TWO-SOURCE INCLUSION RULE in the XML doc on FencedCodeBlockRenderer.LanguageMap before adding one — a brush is mapped only when it appears in Atlassian's documented Code Block language list AND is a Prism component id.
STILL FAIL-LOUD (each a backlog item): raw HTML blocks and inline raw HTML, an HTML comment sharing its line with author text, an unterminated comment, indented code blocks, relative non-.md links, relative .md with no/unresolved resolver, title tooltip on an internal page link, local image with no/unresolved attachment resolver, empty image source, 'data:' URI image, image followed by '{width=...}', rich alt text carrying a nested link or image, mermaid with no/failing resolver or an empty body or info-line attributes, the diagram dialects plantuml/puml/graphviz/dot/d2, an emphasis delimiter other than * _ or ~~, '[TOC][TOC]', and anything without a dedicated renderer (UnknownConstructRenderer, registered LAST).
NOTE THE ASYMMETRY the render pass exposes: a mermaid fence whose RESOLVER fails throws InvalidOperationException (not NotSupportedException), so it CRASHES an acceptance run rather than becoming a row. Harmless today because WikiTree.DiagramResolver never returns null, but any test page with a mermaid fence must pass a real diagram resolver, not '_ => null'.
DELIBERATELY NOT FAIL-LOUD (decided, tested, do not "fix"): an unknown PROGRAMMING fence language degrades to an unlabelled code macro; a mixed task list degrades to a plain list with literal markers; a same-page #anchor link degrades to text only; a dropped inline comment leaves the author's double space; an unrecognized character reference publishes as escaped literal text; table alignment, ordered-list start offset and NOTE-vs-IMPORTANT are dropped.
DIAGNOSTICS: SEVEN codes — unknown-fence-language, mixed-task-list, same-page-anchor-link, table-alignment-dropped, ordered-list-start-dropped, alert-type-collapsed, task-list-numbering-dropped. THE SINK IS INERT BY CONTRACT — Convert's output is byte-identical with and without it, pinned by Convert_without_a_sink_emits_identical_storage_format, which protects the 27 hand-reviewed goldens (§4.3); they call Convert with the body plus three POSITIONAL resolvers, so 'diagnostics' MUST stay the last optional parameter. A THROW DOES NOT DISCARD EARLIER DIAGNOSTICS OR DIAGRAMS.
ACCEPTANCE: DocuMe.Core.Acceptance — ConversionAcceptance.Run/RunTree/RunDirectory, AcceptanceReport (Pages, Failures, Diagnostics, Diagrams, Renders, PageCount/FailedPageCount/DiagnosticCount/WarningCount/MeetsAcceptanceBar), AcceptancePolicy + DiagnosticSeverity, MermaidAcceptance.RunAsync/RenderDiagramsAsync, DiagramRenderReport (Count/DistinctCount/FailedCount/FailedPageCount/ByDialect/AllRendered), QuotedTokens. CLI: 'docume convert <wiki-root> [--accept <code>] [--render-mermaid] [--renderer <path>]', exit 0/1.
LINK MAP: WikiTree.Load validates the tree and fails loud on a missing/duplicate title, a bad extraPages path, or a flatten collision; resolvers return null (-> converter fails loud) for above-root, root-absolute, excluded, directory and missing targets; attachment names are flatten(wiki-root-relative path); the mermaid resolver never returns null, which is exactly why the render pass exists.
VERSIONS PINNED: Markdig 1.3.2, YamlDotNet 18.1.0, Microsoft.Extensions.FileSystemGlobbing 10.0.10, System.CommandLine 2.0.10, Spectre.Console 0.57.2, beautiful-mermaid 1.1.3 (npm devDependency), Microsoft.Extensions.Http.Resilience 10.8.0, WireMock.Net 2.13.0 (tests).

=== HOUSEKEEPING (not blockers) ===
An untracked 'tests/golden/.claude/analytics.json' exists — an MTK analytics write that landed under a relative path while the cwd was tests/golden. HARMLESS (every golden discovery globs '*.md') and deliberately NOT deleted, since the global rule forbids deleting data files without Mirko's say-so. If Mirko wants it gone: 'rm -r tests/golden/.claude'.
'node_modules/' is gitignored; package-lock.json IS committed, so 'npm ci' is reproducible.
